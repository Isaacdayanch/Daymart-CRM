# Daymart CRM — Memoria del proyecto

> Nota técnica: este proyecto usa Next.js. Antes de tocar código de Next.js, revisa `AGENTS.md` (reglas específicas del framework, se regenera solo).

## Quién es el usuario y cómo trabajar con él

Isaac, dueño de Daymart. **No sabe programar.** Reglas de trabajo obligatorias:

- Hablarle siempre en **español mexicano**, simple y directo, sin jerga técnica. Si se usa un término técnico, explicarlo ahí mismo.
- Antes de construir algo grande: proponer el plan y **esperar su "va"** antes de tocar código.
- Mostrar los cambios y pedir aprobación conforme se avanza. Ir paso a paso, sin adelantarse.
- Dar recomendaciones activamente, no solo ejecutar lo que pide. Si hay una mejor forma de hacer algo, decírselo.
- Si necesita instalar algo o correr comandos, guiarlo clic por clic. No asumir que sabe hacerlo.
- **Cada vez que se le pida correr un SQL, darle el link directo al editor SQL de Supabase** para que solo le pique: **`https://supabase.com/dashboard/project/sytynlembvdqsddvatcg/sql/new`** (ID de su proyecto: `sytynlembvdqsddvatcg`, lo compartió Isaac). Pasos: link → pegar el SQL → botón "Run".
- Le importa mucho la **portabilidad**: no quiere quedar atado a una tecnología. Por eso el stack elegido usa piezas estándar (ver abajo) — sus datos y su código deben ser exportables/movibles en cualquier momento, sin proveedor que lo tenga "secuestrado". Nada del sistema es definitivo: campos, pantallas y cálculos se pueden agregar/editar/quitar cuando Isaac lo pida, con el mismo proceso (mostrar el cambio, esperar su aprobación).

## El negocio

**Daymart** — e-commerce de importación. Isaac importa productos desde China (equipo de gym/fitness, artículos para el hogar, espejos, cocina, exteriores) y los revende en Mercado Libre y Amazon México. Negocio en crecimiento.

## Visión general del sistema

Un CRM/sistema de gestión propio para Daymart:

- Web app, responsive (se ve bien en compu y celular), sin apps que descargar.
- Diseño limpio, bonito, amigable y eficiente.
- Crece por módulos con el tiempo. Meta final: manejo de stock, conexión API con Mercado Libre (luego Amazon MX), ventas, y análisis.

### Navegación general

- **`/` es un dashboard de bienvenida**, no la lista de contenedores. Muestra "Bienvenido a Daymart CRM", valor de inventario (Stock) y saldo en Finanzas (dueño) o productos sugeridos para reordenar (operadora), y accesos rápidos a Contenedores/Stock/Finanzas. Sin número de contenedores — a Isaac no le sirve ese dato ahí.
- **La lista de contenedores vive en `/contenedores`** (antes era la home). El botón "+ Nuevo contenedor" está ahí mismo, no en el menú. Arriba, junto al título, hay un selector **"Vista general | Vista de productos"** (`selector-vista.tsx`): la segunda (`/contenedores/productos`) muestra TODOS los productos que vienen en camino, agrupados por contenedor (título con número, estado y proveedor), en Lista (foto, producto, SKU, categoría, piezas, cajas, CBM, costo/pza si es dueño) o Galería — la vista "de un guamazo" que Isaac tenía en Sheets. Muestra TODOS los contenedores del más nuevo al más viejo (Isaac pidió explícitamente ver también los anteriores hacia abajo, el último siempre hasta arriba); el link "Ver solo lo que viene en camino" (`?encamino=1`) filtra a los que todavía no llegan a bodega. Solo lectura: no se edita nada desde ahí.
- **Header minimalista en todas las pantallas**: logo a la izquierda, menú ☰ a la derecha, nada de links de texto sueltos ("Stock", "Volver a la lista", etc.) — toda la navegación entre secciones pasa por el menú.
- **Menú ☰ solo con las secciones raíz**: Finanzas (dueño, hasta arriba), Contenedores, Stock, Usuarios (dueño). Lo que es específico de una sección vive DENTRO de ella, no en el menú: "Nuevo contenedor" y "Papelera" están dentro de Contenedores, "Dar salida" está dentro de las pestañas de Stock.
- **Orden de pestañas de Finanzas** (Isaac, 17 sep): Resumen, Movimientos, Cuentas, Proveedores, Maaser, Facturas, Prestamistas, Socios, Categorías, Balance (Balance al final porque no lo mencionó).

### Stack elegido (aprobado por Isaac — "si va")

| Pieza | Qué es | Por qué |
|---|---|---|
| **Next.js** | Motor de la app web | Estándar de la industria, responsive automático (compu y celular) |
| **Supabase** | Base de datos (PostgreSQL) + login + almacenamiento de imágenes | Gratis para empezar, datos 100% exportables, formato estándar (Postgres) |
| **Vercel** | Hosting — donde "vive" la app en internet | Gratis para empezar, acceso vía link desde cualquier navegador, sin instalar nada |

**Principio de portabilidad:** todo el código vive en GitHub (propiedad de Isaac). La base de datos es PostgreSQL estándar, exportable en cualquier momento. Ninguna pieza es propietaria/cerrada — si aparece mejor tecnología, se puede migrar sin perder nada.

### Marca

Logo de Daymart en `src/components/logo.tsx` — solo texto ("Daymart" + tag "CRM" al lado, misma tipografía), sin ícono (Isaac pidió quitar la figura de casita que se había recreado al inicio). Aparece en el encabezado de todas las pantallas (incluidas las que se imprimen). Tipografía: Baloo 2 (Google Font, redondeada), cargada en `src/app/layout.tsx`. Nota histórica: Isaac mandó la imagen del logo real por el chat, pero en este ambiente remoto las imágenes pegadas ahí no llegan como archivo al disco — solo se pueden ver, no guardar. Si algún día consigue el archivo original y lo sube como archivo (no pegado en el chat), se puede retomar la idea del ícono con el diseño real.

**Header de inicio**: minimalista, tipo Apple — sticky con blur, solo el logo a la izquierda y un menú ☰ a la derecha. "Nuevo contenedor", "Stock" y "Papelera" viven dentro del menú desplegable (`src/app/menu-mas.tsx`), no como botones sueltos.

## Módulo 1 (en construcción): Pedidos / Contenedores

Flujo de "hacer un pedido en China". Todavía NADA de ventas, stock ni Mercado Libre (eso viene después).

Fuente de referencia: Excel "Proveedores Dale Click", hoja "Daymart". Se leyeron las fórmulas reales de los contenedores 10 al 15 (filas 244–313 aprox.; ya hay huecos preparados para 16, 17, 18).

### Estructura de dos niveles

**CONTENEDOR** (nivel 1):
- Número de contenedor (ej. 11, 12, 13...)
- Booking (ej. MRKU2892234) — ya no se usa "barco", se quitó por petición de Isaac
- Estado — lista fija, en el orden real del proceso: Configurándose → En tránsito → Recibido en puerto → Liberado de aduana → Recibido en bodega
- **Flete**: en dólares, con su propio tipo de cambio (se paga de una sola vez)
- **Aduana**: en pesos, sin tipo de cambio (ya se cobra en pesos)
- **Mercancía**: se paga en varios abonos a lo largo del tiempo, cada uno puede tener un tipo de cambio distinto. Por ahora los abonos son por contenedor completo (no por producto individual — eso se dejará para un sistema de pagos más completo en el futuro). El sistema calcula solo el tipo de cambio promedio ponderado (total pesos ÷ total dólares de los abonos).
- **Fábrica/Proveedor principal**: campo a nivel contenedor. Rellena automáticamente el campo de cada producto nuevo (la mayoría de los contenedores son de un solo proveedor), pero se puede cambiar por producto si el contenedor es consolidado (varios proveedores). No se guarda como lista aparte — el resumen de "proveedores en este contenedor" se calcula solo, sacado de los productos reales.
- **Documentación** (pestaña aparte): checklist con 6 documentos fijos — Telex, Packing list (proveedor), Invoice (proveedor), Telex release, BL, HBL. Cada uno se sube como archivo (bucket privado de Supabase Storage, acceso con link firmado temporal). Barra de progreso (X de 6), se pone verde cuando está completo. Pensado para que, cuando haya login, el equipo de Isaac (aduanal, importación) pueda cargar/ver esto también con permisos — por ahora un solo usuario, sin permisos diferenciados todavía.
- Resumen calculado: costo por producto y costo total del contenedor

**PRODUCTO** (nivel 2, varios por contenedor):
- Categoría
- Fábrica (empresa: TOPKO, Union Chance, Zhoya...) y Proveedor/contacto (persona: Sarah Kuo, Fairy...) — ambos campos, contacto opcional
- Imagen del producto
- SKU: autogenerado (categoría + nombre, como su fórmula actual), editable a mano
- Nombre del producto (se usará para manejar stock más adelante)
- Cantidad
- Memo/detalles para el proveedor (color, código, comentario)
- Precio en dólares
- Piezas por caja (PC PER CTN)
- Medidas de la caja: largo, ancho, alto (cm)

### Cálculos (confirmados contra las fórmulas reales del Excel)

- **Cartones** = Cantidad ÷ Piezas por caja
- **CBM por producto** = (largo × ancho × alto en cm ÷ 1,000,000) × número de cartones
- **Total USD por producto** = precio USD × cantidad
- **Flete en pesos** = Flete USD × tipo de cambio del flete
- **Tipo de cambio promedio de mercancía** = (suma de monto_USD × tipo_cambio de cada abono) ÷ (suma de monto_USD de todos los abonos)
- **Costo por CBM del contenedor** = (Flete en pesos + Aduana) ÷ CBM total real del contenedor
  *(Nota: en el Excel original esto a veces se dividía entre un número de CBM escrito a mano en vez del CBM real — en el sistema nuevo siempre se usa el CBM real, calculado automáticamente. Esto corrige un error manual que existía.)*
- **Gasto repartido por pieza** = (CBM del producto × Costo por CBM del contenedor) ÷ Cantidad del producto
- **Costo final por pieza (pesos)** = Gasto repartido por pieza + (Precio USD del producto × tipo de cambio promedio de mercancía)

### Decisiones ya tomadas

1. Fábrica Y proveedor/contacto: los dos campos, contacto opcional.
2. SKU autogenerado (categoría + nombre) pero editable a mano.
3. Gastos del contenedor: Flete (USD + su tipo de cambio), Aduana (pesos, sin tipo de cambio), Mercancía (abonos en USD, cada uno con su tipo de cambio, promedio ponderado calculado por el sistema).
4. El tipo de cambio "de los productos" es el promedio ponderado de los abonos de mercancía del contenedor — no un campo único fijo.
5. Abonos de mercancía por ahora son por contenedor completo, no por producto (eso se dejará para un sistema de pagos más adelante).
6. Estado del contenedor: campo de primer nivel (por contenedor), lista fija en el orden real del proceso (ver arriba).
7. Campo "Barco" eliminado — solo se usa Booking.
8. Vista de productos: tabla o galería (fotos), Isaac elige con un botón. Ambas muestran el costo final por pieza ya calculado.
9. Packing list: vista de impresión (usa "Imprimir → Guardar como PDF" del navegador, sin librería de PDF), con opción de "con precios" / "sin precios". Un PDF generado por el sistema con diseño propio (logo, etc.) se deja para más adelante si esta versión no basta.
10. Permisos/colaboradores todavía NO se construyen (no hay login). Isaac ya avisó que los quiere más adelante para que su equipo (aduanal, importación) pueda cargar documentos con permisos — dejarlo anotado como pendiente, no construir hasta que se hable con calma. Técnicamente no está "loco" — Supabase Auth ya trae login/roles integrado, es trabajo normal cuando se defina qué rol ve/edita qué.
11. Historial de fechas: cada vez que cambia el estado del contenedor (desde el menú rápido o el formulario grande), se guarda automático en una tabla de historial (`historial_estados_contenedor`). En el contenedor se muestra una línea de tiempo con la primera fecha en que se alcanzó cada estado.
12. Productos: se pueden editar todos sus campos (incluida la foto) después de creados, y se pueden reordenar a mano (subir/bajar) — el orden se guarda en la base de datos (columna `orden`), no es solo visual.
13. Restock: al agregar un producto, hay un selector "¿ya lo has traído antes?" con todos los productos de TODOS los contenedores (uno por SKU, el más reciente). Al elegir uno, se rellenan solos categoría/fábrica/proveedor/nombre/SKU/memo/precio/piezas/medidas/foto — la cantidad se deja vacía porque siempre cambia. Es cálculo directo sobre la tabla `productos` existente, no hay tabla de catálogo aparte todavía.
14. Subida de fotos: si falla, ya no se guarda en silencio — se le avisa a Isaac con el error exacto de Supabase Storage (antes el error se ignoraba, por eso los productos guardaban "sin foto" sin explicación).
15. "Otros gastos" del contenedor: campo opcional (dólares + su propio tipo de cambio), en un bloque desplegable cerrado por defecto (no siempre aplica). Para fletes internos en China u otros cargos. Se reparte por CBM junto con flete y aduana.
16. Borrar un contenedor NUNCA es inmediato: es información valiosa. Se pide escribir el número del contenedor para confirmar, y el borrado real es un "soft delete" (columna `eliminado_en`) — se manda a una Papelera (`/papelera`) de donde se puede Restaurar o Borrar definitivo (con la misma confirmación de escribir el número). Sin límite de tiempo/borrado automático — se queda ahí hasta que Isaac decida a propósito. La lista principal (`/`) filtra `eliminado_en is null`.
17. Límite de subida de archivos: Next.js Server Actions limitan el tamaño del formulario a 1 MB por defecto, lo cual rechazaba fotos de celular. Se subió a 10 MB en `next.config.ts` (`experimental.serverActions.bodySizeLimit`). Si algún archivo sigue sin subir, revisar esto primero.
18. **Crédito del proveedor chino por contenedor** (migración 0026, caso real del contenedor 15: adelanto pagado, el resto a 60 días de que sale de China):
    - `contenedores.credito_dias` (opcional, campo "Días de crédito del proveedor" en Nuevo contenedor y en Editar). Los días corren desde la fecha en que el contenedor pasa a **"En tránsito"** — así lo confirmó Isaac ("desde que lo cargan / sale de la bodega del proveedor"). Al elegir "En tránsito" en la tarjeta de estado de un contenedor CON crédito, se pregunta la fecha exacta de salida (hoy por defecto, calendario editable) y se muestra la fecha de vencimiento calculada antes de confirmar (`cambiarEstado` acepta `fechaSalida`, que también se guarda en el historial).
    - El saldo a crédito se captura como un abono de mercancía **"Pendiente"** con tipo de cambio ESTIMADO — el costo por pieza ya usa todos los abonos (pagados y pendientes) en `tipoCambioPromedioMercancia`, así que Isaac puede fijar precios con el costo completo del contenedor desde el día uno, antes de pagar. Al pagarlo, el tipo de cambio real reemplaza al estimado y `recalcularCostoEntradasContenedor` refresca el stock.
    - `aplicarCreditoProveedor` (contenedores/[id]/actions.ts): a cada abono pendiente le pone `pagos_mercancia.fecha_limite` = salida + credito_dias y le genera (una sola vez) un CARGO en `movimientos_deuda_proveedor` a nombre de la fábrica principal, en USD, con esa fecha límite y `contenedor_id` — así aparece en Finanzas → Proveedores, en "Debes" y en el aviso amarillo de 7 días. El cargo queda ligado desde el abono (`pagos_mercancia.cargo_deuda_id`), mismo patrón que `movimiento_financiero_id`: si se edita el monto/fecha límite del abono se actualiza el cargo; si se borra el abono, se borra el cargo. Se dispara al pasar a En tránsito, al guardar el contenedor y al agregar un abono (si ya salió de China).
    - **Pagarlo**: en Finanzas → Proveedores → "Enviar desde una cuenta puente", al elegir el contenedor aparece "¿a qué abono aplica este pago?" con sus abonos pendientes (`abono_pendiente_id`): `registrarEnvioCuentaPuente` ACTUALIZA ese abono (pagado, tipo de cambio real = pesos+comisión ÷ dólares, cuenta, movimiento) en vez de insertar uno nuevo — si no, los dólares se duplicarían en el promedio. El ABONO a la deuda del proveedor cierra el cargo por FIFO. Por eso, en el contenedor, un abono pendiente con cargo NO se puede marcar "Pagado" a mano (`actualizarAbono` lo ignora): se paga desde Finanzas para que el dinero real y la deuda queden en un solo registro.
    - `fila-abono.tsx`: cada abono tiene "editar" (monto, tipo de cambio, fecha, y fecha límite o estado según el caso) y muestra "vence dd/mm" en los pendientes con crédito. Tarjeta ámbar "Crédito del proveedor" en el contenedor con días, USD pendiente y fecha de vencimiento.
    - **"En tránsito" siempre pregunta la fecha de salida** (hoy por defecto, calendario editable), tenga o no crédito — es la fecha real del embarque; si no hay crédito, el mismo aviso le recuerda a Isaac ponerlo primero en Editar. **Regresar un contenedor a un estado anterior limpia lo posterior** (`registrarHistorialSiCambia`): Isaac marcó "En tránsito" por error y al devolverlo a "Configurándose" el historial seguía diciendo "En tránsito 15 sep". Ahora al retroceder se borran las fechas de los estados posteriores (se conserva la fecha original del estado al que se regresa), y si se regresa a antes de "En tránsito" se deshace el crédito (`revertirCreditoProveedor`: quita `fecha_limite` y borra el cargo de Finanzas de los abonos pendientes). Al avanzar a un estado que tenía una fecha vieja de un intento anterior, se reemplaza por la nueva; y guardar el contenedor sin cambiar de estado también purga fechas "del futuro" que hayan quedado.
19. **Flete / aduana / otros gastos "estimados"** (migración 0027: `contenedores.flete_estimado`, `aduana_estimada`, `otros_gastos_estimado`): mientras el contenedor se configura, Isaac no sabe el flete ni la aduana reales pero necesita el costo por pieza para fijar precios — pone su estimación y marca la casilla "Es estimado (todavía no tengo la factura real)" junto a cada monto (en Nuevo contenedor y en Editar). El cálculo no cambia (siempre usa el monto que esté capturado); la casilla solo evita que un estimado se confunda con la factura real: las tarjetas "Costo por CBM" y "Costo total contenedor" se pintan en ámbar y aparece un aviso "Incluye montos estimados: flete, aduana…" hasta que Isaac pone el real y desmarca la casilla. Al guardar, `recalcularCostoEntradasContenedor` ya refresca el costo del stock si el contenedor estaba recibido.

## Módulo 2 (en construcción): Stock / Inventario

Fuente de referencia: Excel "Stock y Envíos Daymart", hojas Stock/Entradas/Salidas. Se dejaron fuera del alcance: "Etiquetas" (herramienta de impresión de códigos para Mercado Libre, no es inventario), "Cotización" (lista de ideas de compra futura, no es stock real), "Temporadas" (Isaac pidió explícitamente dejarla para el futuro bloque de análisis), y el tablero de finanzas personales que estaba mezclado en columnas sueltas de la hoja Stock.

### Decisiones tomadas

1. **Bodegas**: tabla propia, empieza con "Bodega Principal" pero se pueden agregar más en `/stock/bodegas`. Cada movimiento de stock queda ligado a una bodega.
2. **Un solo libro de movimientos** (`movimientos_stock`, tipo ENTRADA o SALIDA) en vez de tablas separadas de entradas/salidas — más fácil de auditar todo junto. El stock actual y el valor de inventario NUNCA se guardan a mano, siempre se calculan sumando/restando este libro.
3. **Entrada automática al recibir un contenedor**: elegir "Recibido en bodega" en el estado ya no es un clic — abre `/contenedores/[id]/recibir`, donde Isaac confirma cuánto llegó de verdad de cada producto (por defecto, lo mismo que se pidió). Al confirmar: se generan movimientos de ENTRADA con el costo real por pieza (recalculado con las cantidades reales, para que el CBM/flete/aduana se reparta sobre lo que de verdad viajó), se ajusta `productos.cantidad` a lo real, y se marca `contenedores.stock_generado_en` para no duplicar si se vuelve a tocar el estado.
4. **Mercancía pendiente en China**: si algo no llegó completo, la diferencia se guarda en `pendientes_china` (con todos los datos del producto, si ya está pagada, y de qué contenedor viene). Se ve en `/stock/pendientes`. Al armar un contenedor futuro, en "Agregar producto" hay un selector "¿es mercancía pendiente de China?" que precarga esos datos; al guardar, el pendiente se cierra (o se reduce si solo se consolidó una parte).
5. **Costo de inventario**: cada SKU tiene un costo promedio ponderado (de todas sus entradas históricas). Valor de inventario = stock actual × costo promedio. En el contenedor, una vez recibido, se muestra una tarjeta comparando el costo total del contenedor contra el valor que realmente entró a stock — si no cuadra, es señal de que falta un abono o hay mercancía pendiente sin registrar.
6. **Reórdenes por rotación real**: cada SKU calcula su rotación diaria (salidas de los últimos 90 días ÷ 90) y su punto de reorden = rotación × "tiempo de espera" (días que tarda en llegar un pedido nuevo, configurable en `/stock/configuracion`, empieza en 60 días). Se marca "sugerido reordenar" cuando el stock actual cae a ese punto o menos.
7. **Salidas**: pantalla propia en `/stock/salidas` (sin login todavía, cualquiera con acceso puede registrar). Isaac arma una lista de líneas (producto, cantidad, categoría, notas) y las registra todas juntas de un clic — no una por una. Categorías tomadas de su Excel: Full (con color de etiqueta libre — Isaac organiza sus Full por color: verde, morado, blanco, azul, naranja...), Piezas (venta directa en bodega que recoge Mercado Envíos), Paquetería (venta fuera de ML, se manda por FedEx/etc.), Muestra, Ajuste (se resta, como las anteriores) y **Devolución** — caso especial: en vez de restar, SUMA la cantidad de vuelta al stock (es mercancía que regresa), guardada internamente como un movimiento `AJUSTE` positivo con el costo promedio actual del SKU. La "firma" de salida con encargado, permisos y auditoría se deja pendiente hasta que Isaac delegue esa tarea a alguien (ver abajo): requiere login real primero. El selector de producto (`selector-producto.tsx`) es un desplegable propio (no un `<select>` nativo, que no puede mostrar imágenes) — muestra la foto de cada producto en la lista y tiene buscador, para identificarlos rápido a simple vista.
8. **Stock en Full** (lo que Mercado Libre tiene en sus centros de distribución) se deja para el Módulo 3, cuando se conecte la API de Mercado Libre — ahí se podrá reconciliar automáticamente contra lo que salió de bodega.
16. **Fecha de recepción editable**: al recibir un contenedor (no al editar una recepción ya hecha), hay un campo de fecha con hoy por defecto pero editable — pensado para cargar contenedores históricos (13, 12, 11, 10) con su fecha real. Esa fecha se usa para el historial de estados del contenedor Y para `creado_en` de las entradas de stock generadas, así el inventario y sus fechas quedan consistentes con la realidad, no con "hoy".
9. **Cajas**: cada movimiento guarda las piezas por caja del producto en ese momento; el resumen de stock muestra piezas primero y cajas después (columna discreta, no compite con la cifra principal). Se calcula, no se guarda a mano.
10. **Hoja de revisión de inventario** (`/stock/imprimir`): imprimible, sin precios ni costos — solo foto, SKU, producto, piezas y cajas según el sistema, más una columna en blanco para anotar el conteo físico. Pensada para auditar bodega a mano. **Versión "sin cantidades" para bodega** (`?sin=1`, 25 sep; Isaac: "que mi gente solo me mande la cantidad y notas, yo hago el match en la oficina"): esconde piezas/cajas del sistema y deja columnas en blanco "Piezas contadas" y "Notas (merma, dañado, cajas abiertas…)" + línea "Contó: ____"; conteo a ciegas. Selector arriba "Con cantidades del sistema | Sin cantidades (para bodega)" y link "Hoja para bodega (sin cantidades)" en la tabla de Stock (respeta el filtro de categoría).
14. **Fotos en Stock**: cada movimiento guarda también la foto del producto (igual patrón que piezas por caja — se toma la más reciente de ENTRADA/AJUSTE). Se ve en el listado de `/stock` y en la hoja imprimible.
15. **Foto en "Agregar stock manual"**: se puede subir una foto directo ahí (mismo componente `CampoImagen` que usan los productos de contenedor, mismo bucket `productos` de Storage) — pensado para mercancía que nunca llegó en un contenedor formal y por eso no tiene foto en el catálogo.
17. **Bug corregido — texto "borroso"/gris en formularios**: la plantilla original de Next.js traía un modo oscuro automático (`@media prefers-color-scheme: dark`) que cambiaba el color del texto a gris claro cuando el celular/compu de Isaac estaba en modo oscuro — pero las tarjetas siguen siendo blancas siempre (la app no tiene diseño oscuro), así que el texto quedaba gris clarito sobre blanco, casi ilegible. Se quitó ese bloque de `globals.css`: el texto ahora es siempre oscuro, sin importar el modo del sistema.
18. **Bug corregido — fecha de recepción no editable después de la primera vez**: el campo de fecha solo aparecía al recibir un contenedor por primera vez, no en "Editar recepción". Ahora aparece siempre, precargada con la fecha que ya tenía guardada; si Isaac la cambia, se actualiza en los tres lugares donde queda registrada (`contenedores.stock_generado_en`, el historial de estados, y `creado_en` de las entradas de stock de ese contenedor).
19. **Componentes de UI propios en vez de los nativos del sistema operativo**: Isaac pidió que los desplegables y el calendario se vean "bonitos, tipo Apple", no como los del navegador/celular. Se construyeron componentes reutilizables en `src/components/`:
    - `Selector`: reemplaza `<select>`. Panel flotante redondeado con sombra, funciona "sin controlar" (como un select normal, con `defaultValue` + `name`, manda el valor con un input oculto).
    - `CampoSugerencias`: reemplaza `<input list="...">` (datalist nativo). Campo de texto libre con panel de sugerencias propio, filtrable.
    - `CampoFecha`: reemplaza `<input type="date">`. Calendario propio (mes/año navegable, grid de días), sin depender del picker del sistema operativo.
    - `CampoMonto`: para TODO campo de monto de dinero en el sistema (contenedores, productos, abonos, Finanzas, Research, Stock) — signo de $ fijo a la izquierda y separador de miles EN AUTOMÁTICO mientras se escribe (no solo al salir del campo, que es lo que hacía `CampoNumero` antes) — Isaac lo pidió explícitamente porque en montos grandes ("1,000,000 de pesos") se le perdía cuántos ceros llevaba. Funciona controlado (`value`+`onChange`, para formularios que ya traían el monto en un `useState` para calcular algo en vivo, ej. Research) o sin controlar (`defaultValue`+`name`, como los demás). El valor que de verdad viaja al formulario/servidor siempre es el número limpio sin comas (por un input oculto), igual que ya hacía `CampoNumero`. `CampoNumero` (sin $, formatea solo al salir del campo) se dejó para campos que NO son dinero pero sí numéricos con decimales, como tipo de cambio, cantidad, piezas por caja y medidas en cm — ahí no aplica el signo de $ y nunca llegan a miles, así que no hacía falta cambiarlos.
    Ya no queda ningún `<select>`, `<input type="date">`, `datalist` nativo, ni un campo de monto de dinero sin $/comas en toda la app.
22. **Orden del formulario "Agregar producto"**: primero el selector de restock (con foto de cada producto en la lista), luego los campos normales (con foto propia), y hasta abajo — dentro de un `<details>` colapsado, discreto — la pregunta "¿es mercancía pendiente de China?". Antes esa pregunta iba primero y en una caja amarilla grande, y Isaac dijo que le estorbaba al llenar un producto normal (el caso más común).
23. **Fecha de pago en abonos de mercancía**: al agregar un abono (en "Nuevo contenedor" y dentro de un contenedor ya creado) se captura la fecha de pago (hoy por defecto, editable). Los abonos ya guardados también se pueden corregir de fecha: se le da clic a la fecha en la lista y aparece el campo para cambiarla (`actualizarFechaAbono`). La columna `fecha` en `pagos_mercancia` ya existía desde el inicio pero no se usaba en la interfaz.
24. **Bug corregido — "Valor que entró a stock" en $0**: el costo por pieza que entra a stock se calcula al momento de recibir el contenedor, con el flete/aduana/abonos que haya capturados EN ESE MOMENTO. Si Isaac llena esos gastos después de recibir (algo normal — la mercancía suele llegar antes de que se terminen de pagar/capturar los abonos), el costo que ya quedó guardado en las entradas de stock se quedaba congelado en 0 y nunca se refrescaba. Ahora `recalcularCostoEntradasContenedor` se dispara solo cada vez que se guarda el contenedor (flete/aduana/otros gastos) o se agrega/quita un abono — vuelve a calcular el costo por pieza de las entradas ya existentes (sin duplicar ni mover cantidades) con los datos actuales. También hay un botón manual "Recalcular costo →" en la tarjeta de reconciliación del contenedor, por si Isaac quiere forzarlo.
25. **Bug corregido — "Recalcular costo" no encontraba el movimiento a actualizar**: la primera versión buscaba el movimiento de stock a corregir por SKU, pero si Isaac editaba el SKU de un producto DESPUÉS de recibir el contenedor (algo que sí hace), el movimiento guardado tenía el SKU viejo y la búsqueda no encontraba nada — no daba error, simplemente no actualizaba nada, en silencio. Se agregó `producto_id` a `movimientos_stock` (columna real, migración 0013) que se guarda desde que se crea el movimiento (`confirmarRecepcion`, `editarRecepcion`) y ya no depende del SKU. Para movimientos viejos sin `producto_id` (los que ya tenía Isaac cargados), la función busca por SKU como respaldo y de una vez les rellena el `producto_id`. El botón "Recalcular costo →" ahora también dice cuántos productos sí actualizó (o el error real), en vez de un mensaje mudo de "listo".
26. **Bug de raíz corregido — la recepción podía "funcionar" sin guardar nada de stock**: `confirmarRecepcion`, `editarRecepcion` y `agregarStockManual` insertaban en `movimientos_stock` sin revisar si el insert había fallado. Si fallaba (por lo que fuera), el contenedor de todos modos se marcaba "Recibido en bodega" con fecha y todo, pero el libro de movimientos se quedaba vacío para ese contenedor — exactamente lo que le pasó a Isaac con los contenedores 12 y 13 (el 14 sí se guardó bien). Ahora:
    - Los tres insertan y SÍ revisan el error; si falla, no se marca el contenedor como recibido y se muestra el error en la propia pantalla de recepción (`?error=` en `/recibir`), en vez de fingir que quedó bien.
    - "Recalcular costo →" ahora también repara el pasado: si un contenedor ya está "Recibido en bodega" pero no tiene NINGUNA entrada de stock guardada, genera las entradas de cero con los productos, costos y bodega (la primera activa) actuales, y avisa "No tenía ninguna entrada guardada — se generaron X de Y de cero." Isaac lo usó para reparar los contenedores 12 y 13.
27. **"Explicar diferencia" con mercancía pagada en China**: Isaac se dio cuenta de que a veces la diferencia entre el costo del contenedor y el valor que entró a stock no es un error — es mercancía de ese contenedor que se quedó pendiente en China pero YA se pagó (sale en `/stock/pendientes`, marcada "Pagada"). Junto al botón manual "+ Explicar esta diferencia" ahora hay uno nuevo, "Fue mercancía pagada en China →" (`calcularPendienteChinaPagado` en `contenedores/[id]/actions.ts`, cálculo en `src/lib/calculos-pendientes.ts`): busca los pendientes de ESE contenedor con `pagado = true` y `estado = 'PENDIENTE'`, calcula su valor con la misma fórmula de costo por pieza que un producto normal (CBM repartido + precio USD al tipo de cambio promedio de mercancía), y prellena el monto y la nota del ajuste — Isaac revisa y le da "Guardar" para confirmarlo, nunca se guarda solo.
29. **Editar/quitar un movimiento suelto en `/stock/movimientos`** (`actualizarMovimientoStock`/`eliminarMovimientoStock`, `acciones-movimiento.tsx`, solo dueño): caso real de Isaac — registró una venta como "salida" en Stock, luego la volvió a registrar bien desde Ventas y necesitaba quitar la salida vieja. Solo aplica a movimientos SIN `contenedor_id` ni `venta_id` (los capturados a mano: salidas, ajustes, entradas manuales); los que vienen de un contenedor o de una venta se corrigen desde su origen (mismo principio que los movimientos "ligados" de Finanzas). Se puede corregir cantidad, fecha y nota; quitar se bloquea si el stock del SKU quedaría en negativo. `movimientos_stock.venta_id` se agregó a `MovimientoStock` en tipos.
28. **Corregir el costo de una entrada cargada a mano** (`actualizarCostoManual`): si Isaac agrega stock con "Agregar stock manual" (o "Carga masiva") y deja el costo en $0 o mal, en `/stock/producto/[sku]` → tabla "Movimientos" → columna "Costo/pza" hay un "editar" junto al costo de esa entrada (o ajuste positivo) para corregirlo ahí mismo — recalcula solo el costo promedio del SKU. Solo aplica a entradas SIN contenedor; una entrada que sí vino de un contenedor no se puede tocar aquí (se corrige desde el contenedor con "Recalcular costo →", que ya existía) para no pisar ese mecanismo.
20. **Sugerencias de fábrica/proveedor/categoría**: `src/lib/catalogo-proveedores.ts` junta las fábricas, proveedores y categorías que Isaac ya ha usado (a nivel contenedor y a nivel producto) para sugerirlas con `CampoSugerencias` al capturar un contenedor o producto nuevo — normalmente se repiten mucho. El campo "Proveedor principal" (fábrica + proveedor) ahora también existe en "Nuevo contenedor" (antes solo se podía poner editando el contenedor ya creado).
21. **Categoría se recuerda entre productos del mismo contenedor**: al agregar un producto nuevo (no restock, no pendiente de China), la categoría se precarga con la del último producto ya agregado a ese contenedor — casi siempre un contenedor trae puros productos de la misma categoría.
11. **Categorías de salida**: se tomaron tal cual del Excel de Isaac (hoja Salidas, columna "Lugar"): Full, Paquetería, Piezas, Muestra, Devolución, Ajuste. Es un select, no texto libre.
12. **Editar recepción**: si Isaac hace un conteo físico y algo no cuadra con lo que ya se recibió, puede volver a `/contenedores/[id]/recibir` (botón "Editar recepción" en la tarjeta de reconciliación) y corregir cantidades. Esto NO crea entradas nuevas — genera un movimiento de tipo `AJUSTE` con la diferencia (puede ser positiva o negativa), para no duplicar stock. Los ajustes no cuentan como salida real, así que no ensucian el cálculo de rotación/reorden. Por ahora esta capacidad es solo para Isaac — cuando haya más usuarios, se restringe con permisos.
13. **Agregar stock manual** (`/stock/agregar`): para cargar de una vez el inventario de contenedores anteriores al 10 (u otro stock existente) sin tener que recrear el contenedor completo en el sistema. Crea una ENTRADA directa (sin contenedor asociado) con SKU, cantidad, costo por pieza y una nota libre.

### Catálogo de productos y marcas (Fase A, migración 0038 — plan aprobado con "va" el 23 sep)

- **Por qué**: Isaac va a traer marcas propias (Mamey para gym, con líneas de marketing Flow/Strong/Ride/Run/Lift) y quiere que el sistema arme los SKUs. Antes un producto solo "vivía" dentro de cada contenedor (`productos`) o suelto en `movimientos_stock`; ahora hay **un registro por SKU** en `productos_catalogo` (nombre, `marca_id`, `linea`, `categoria`, foto, piezas por caja, medidas, memo) que se alimenta de contenedores, altas manuales y (Fase C) Mercado Libre. Tabla `marcas` (nombre, `codigo` de 2–4 letras, notas, soft delete), semilla Daymart=DAY y Mamey=MAM. `productos.marca_id` opcional. La migración rellena el catálogo con lo que ya existe (último producto por SKU + SKUs que solo viven en movimientos).
- **Regla de SKU nueva** (decidida con Isaac, 23 sep — "en el SKU solo va lo que nunca cambia"): **`MARCA-PRODUCTO-VARIANTE`**, ej. `MAM-BLOQYOGA-GRIS`, `DAY-ESPECUER-120C`. `skuNuevo(codigoMarca, nombre, variante?)` en `src/lib/calculos.ts`: marca = código; producto = 4 letras de las 2 primeras palabras que importan del nombre (se brincan de/para/con/y…, sin acentos); variante opcional recortada a 4. `skuLibre` agrega `-2`, `-3` si ya existe (`skusExistentes` en `src/lib/catalogo.ts` junta catálogo + productos + movimientos). **Ni la categoría ni la línea van en el SKU**: son campos del catálogo para filtrar/reportar (Isaac consideró sublíneas FLO/STR/RID/RUN/LIF y decidió NO meterlas al SKU porque son marketing y pueden cambiar; si un día parte Mamey, asigna la línea a los productos existentes sin tocar SKUs). Los SKUs viejos (`GYM-...`, regla `skuSugerido` = categoría + nombre) se quedan tal cual; `skuSugerido` sigue de respaldo cuando no hay marcas (SQL sin correr) y en Research.
- **Formulario de producto de contenedor** (`campos-producto.tsx`): si no hay marcas (SQL 0038 sin correr) muestra un aviso ámbar en vez de esconder los campos en silencio (Isaac reportó "no veo que cambió" justo por eso); campos nuevos **Marca** (Selector, Daymart por defecto, prop `marcas`) y **Variante** (opcional); el SKU se arma en vivo con `skuNuevo` al cambiar marca/nombre/variante, y deja de armarse solo en cuanto Isaac lo edita a mano o el producto ya traía SKU (editar, restock, pendiente de China). `agregarProducto`/`actualizarProducto` guardan `marca_id` (con respaldo sin la columna si falta el SQL) y llaman `guardarEnCatalogo` (upsert por SKU que solo pisa campos con valor). `agregarStockManual` también guarda en el catálogo.
- **Pantalla `/stock/catalogo`** (pestaña "Catálogo" en `NavStock`): sección **Marcas** (`marcas.tsx`: alta con nombre + código, editar, quitar solo si ningún producto la usa; solo dueño) y **Catálogo** (`fila-catalogo.tsx`: foto, nombre → link a la ficha `/stock/producto/[sku]`, SKU, pastilla de marca o "sin marca", línea, categoría, stock actual; "editar" corrige nombre/marca/línea/categoría → `actualizarProductoCatalogo`, que propaga nombre y marca a `movimientos_stock` y `productos` del mismo SKU; el SKU no se cambia desde ahí). Filtros por marca / "Sin marca (N)" y buscador.
- **Fase B — "Agregar producto" en Stock + histórico (migración 0039: `movimientos_stock.historico boolean`)**, 23 sep (Isaac: "quiero empezar a agregar ya la mercancía y usar Stock activamente"). `/stock/agregar` (pestaña "+ Agregar producto", `formulario-producto-stock.tsx` → `registrarProductoStock`): pestañas "Producto nuevo" (foto, marca, categoría, nombre, variante, SKU que se arma solo con `skuNuevo` y avisa si ya existe, piezas por caja, medidas) / "Ya existe en el sistema" (`SelectorProducto` con foto sobre catálogo + SKUs de stock, muestra el stock de hoy); luego "¿De dónde viene este stock?": **Stock inicial / de antes del sistema** (han entrado X, han salido Y → "quedan Z" en vivo, fecha de corte; se guardan ENTRADA + SALIDA con `historico = true`, referencia "Histórico de antes del sistema", destino "Histórico") o **Entrada suelta** (cantidad, fecha, nota); bodega y costo por pieza (0 = corregir después). Guarda la ficha en el catálogo (marca, categoría, foto, piezas, medidas) y redirige a la ficha del producto. `rotacionDiaria` excluye `historico` (no ensucia el reorden). En la ficha `/stock/producto/[sku]`: botón "Agregar histórico de antes del sistema" (`agregar-historico.tsx` → `agregarHistoricoProducto`; solo dueño y solo si todavía no tiene histórico) con entradas/salidas/fecha/bodega/costo opcional (vacío = costo promedio), vista previa "stock después", y casilla **"Este histórico reemplaza las N piezas que ya habías cargado a mano"** (borra las ENTRADAS sin contenedor/venta/no históricas) para no contar doble; cuando ya hay histórico, la ficha muestra la franja "Antes del sistema: entraron X y salieron Y". El formulario viejo `formulario-stock-manual.tsx` se quitó (`agregarStockManual` sigue en actions para Carga masiva).
- **Histórico sin contar doble lo que ya está en el sistema** (25 sep; caso real: el último contenedor de un banco metió 160 piezas y el total de su hoja ya las incluye). `src/lib/calculos-historico.ts`: `registradoEnSistema(movimientos, excluirManuales?)` = entradas (ENTRADA + ajustes que suman) y salidas (SALIDA + ajustes que restan) NO históricas del SKU; `historicoNeto(totales, registrado, restar)` = lo que de verdad se guarda (totales − registrado, nunca negativo). En la ficha (`agregar-historico.tsx`) y en "+ Agregar producto → Ya existe → Stock inicial" (`formulario-producto-stock.tsx`, `ProductoExistente.entradasSistema/salidasSistema`) aparece la casilla **"Mis totales de la hoja ya incluyen lo que el sistema tiene registrado (entraron N y salieron M): réstalo"** (marcada por defecto si hay algo registrado), con la vista previa "Se guardan como histórico: X entradas y Y salidas" y el stock resultante; viaja como `restar_registrado` y lo aplican `agregarHistoricoProducto` / `registrarProductoStock`. Si además se marca "reemplaza las manuales", esas entradas manuales no se restan (se borran). Así Isaac captura los totales tal cual de su hoja sin restar a mano.
- **Fase C construida (25 sep)**: ver Módulo 5 → "Nuevo producto con estos datos". Recomendación dada: usar el SKU del CRM como "SKU del vendedor" en ML para que la liga sea automática.

## Módulo 3 (en construcción): Finanzas

Solo lo ve Isaac como dueño — bloqueado por completo para el rol "operadora" (ni lectura), igual que `/contenedores/nuevo`, `/usuarios` y `/papelera`.

**Ya construido**: Cuentas (`/finanzas/cuentas`), Categorías (`/finanzas/categorias`), el libro de Movimientos con su panel de registro como pantalla principal (`/finanzas`, historial completo en `/finanzas/movimientos`), y Facturas pendientes de pagar (`/finanzas/facturas`). **Pendiente**: préstamos/prestamistas, proveedores financieros, pagos a China ligados a Finanzas, crédito de contenedor, y el estado de cuenta unificado de deudas.

### Cómo fluye el dinero en Daymart (contexto de Isaac)

1. **Entra**: capital externo, sobre todo préstamos (ej. alguien le presta $1,000,000 MXN). El negocio queda debiendo eso. El dinero entra a una cuenta de Isaac.
2. **Sale a China**: para pagar proveedores chinos, Isaac le da el dinero a un intermediario/proveedor financiero en México que transfiere a China y cobra una comisión (fee, normalmente %).
3. **Trabaja en China**: esos pagos corresponden a contenedores específicos — esto ya existe (`pagos_mercancia`, ver Módulo 1).
4. **Gastos en México**: recibir mercancía, nómina, bodega, logística, etc.
5. **Sueldo**: Isaac retira sueldo (semanal o cuando quiera) y quiere ver cuánto lleva retirado acumulado.
6. **Segunda etapa (NO construir todavía)**: las ventas van a caer en Mercado Pago, de ahí se pasan al banco, y se cotejará ventas vs. costos vs. ganancia con la API de Mercado Libre (Módulo 4). El diseño de Finanzas debe dejar espacio para esto (ej. que se pueda agregar una cuenta tipo Mercado Pago más adelante) sin tener que rehacer nada — pero no se construye ahora.

### Decisiones de diseño (para cuando se apruebe y se construya)

1. **Cero doble captura de pagos a China — Contenedores y Finanzas son UN SOLO dato, no dos que se tienen que parecer**: un pago a China (abono de mercancía) o un abono de crédito de aduana se captura en un solo lugar (la pestaña de Abonos del contenedor, que ya existe) y desde ahí aparece automáticamente como salida de dinero en Finanzas. No hay una pantalla aparte para "registrar el mismo pago otra vez". Mecanismo técnico para que esto no se desincronice (aprendido de los bugs de Stock de este mismo proyecto, donde datos "copiados" se desalinearon):
   - Cada abono (`pagos_mercancia` o el nuevo `pagos_aduana`) guarda el id del movimiento de Finanzas que generó (`movimiento_financiero_id`), no al revés — así siempre hay una relación directa, nunca dos registros sueltos que "deberían" coincidir.
   - Si Isaac corrige el monto, la fecha o la cuenta de un abono ya guardado, el movimiento de Finanzas ligado se actualiza en la misma operación (no se crea uno nuevo suelto).
   - Si Isaac borra un abono, su movimiento de Finanzas se borra con él.
   - Todo insert/update se revisa por error explícitamente (mismo principio que `insertarMovimientosStock` en Stock) — si falla guardar el lado de Finanzas, NO se guarda el abono tampoco (todo o nada), para no repetir el bug de contenedores que se marcaban "recibidos" sin que su stock se hubiera guardado de verdad.
   Mismo mecanismo para el crédito de aduana (ver abajo).
2. **Cuentas** (`cuentas_financieras`): catálogo abierto (Isaac agrega las que quiera) — empieza con "Caja de efectivo" y "Banco BBVA". El saldo de cada cuenta NUNCA se guarda a mano: se calcula sumando/restando su libro de movimientos (mismo principio que el stock).
3. **Un solo libro de movimientos** (`movimientos_financieros`), igual que el stock tiene un solo `movimientos_stock`: cada entrada/salida/transferencia es una fila, con cuenta, monto, fecha, categoría, contraparte y notas. Una transferencia entre cuentas propias (ej. efectivo → banco) es un solo movimiento con cuenta origen y cuenta destino — resta de una y suma a la otra, y NO cuenta como ingreso ni gasto en los reportes (se excluye de esas sumas a propósito).
4. **Categorías** (`categorias_financieras`): catálogo corto y editable por Isaac. Arranca con: Nómina, Bodega, Logística, Importación, Comisiones, Sueldo. "Sueldo" es una categoría fija para poder ver fácil cuánto ha retirado Isaac en total.
5. **Préstamos, súper detallados** (`prestamistas` + `prestamos` + sus abonos): Isaac quiere llevar cada préstamo por persona, no solo un total suelto.
   - **Prestamistas**: catálogo de personas/empresas que le prestan dinero (nombre, notas). Cada prestamista tiene su propia ficha (`/finanzas/prestamistas/[id]`) donde Isaac ve TODOS los préstamos que le ha hecho esa persona a lo largo del tiempo, cuánto le ha prestado en total, cuánto le ha pagado, y cuánto le debe todavía — para saber "a quién le debo y cuánto le he pagado" de un vistazo.
   - **Préstamos**: cada uno ligado a un prestamista, con monto, a qué cuenta entró, fecha, y una **fecha de pago opcional** (puede no tener — deuda abierta sin compromiso de fecha).
   - **Sin interés por ahora**: Isaac no ha tenido ningún préstamo con interés todavía. Cuando le den uno, lo agregamos como una opción más (ej. una tasa opcional por préstamo) — no se construye ahora, pero el diseño no debe estorbar para agregarlo después.
   - **Moneda por préstamo**: cada préstamo se captura en pesos O en dólares (Isaac elige al crearlo) — puede tener préstamos de ambos tipos al mismo tiempo. El abono de un préstamo siempre es en la misma moneda que el préstamo. En todos lados donde se totalice (ficha del prestamista, `/finanzas/deudas`, dashboard) se muestran **dos totales separados** — total en pesos y total en dólares — nunca se convierte ni se mezcla uno con otro.
   - El saldo de cada préstamo NUNCA se edita a mano: se calcula (monto − suma de sus abonos), igual que todo lo demás en el sistema. Cada abono que Isaac registra también genera su movimiento de salida en Finanzas automáticamente (una sola captura).
6. **Estado de cuenta unificado de deudas** (`/finanzas/deudas`): una sola pantalla donde Isaac ve TODO lo que debe en un solo vistazo, sin importar si es un préstamo de una persona o crédito de un proveedor/aduana de algún contenedor — pensada para cuando tenga dinero disponible y necesite decidir a quién pagarle primero:
   - Lista de todo lo que debe: préstamos vivos (por prestamista), crédito de proveedores pendiente (por contenedor), crédito de aduana pendiente (por contenedor).
   - Cada renglón muestra a quién/qué le debe, cuánto, en qué moneda, y su fecha límite (o "sin fecha" si no tiene) — ordenado por fecha límite más próxima primero, para priorizar pagos.
   - Totales separados: cuánto debe en pesos (préstamos + crédito de aduana pendiente) y cuánto debe en dólares (crédito de proveedores pendiente).
7. **Proveedores financieros** (`proveedores_financieros`): catálogo con nombre y fee habitual (%, solo de referencia — ver punto 8 de cómo se captura realmente). Al elegir uno en un pago a China, el % sugerido se autorrellena, pero se puede corregir con el mecanismo del punto 8.
8. **Comisiones — un solo mecanismo, para cualquier movimiento**: en vez de que la comisión solo exista para pagos a China, cualquier movimiento de Finanzas (entrada o salida, ligado o no a un contenedor — pagos a China, pagarle a un proveedor en México, más adelante recibir dinero de ventas) tiene un campo opcional **"¿Tuvo comisión esta transacción?"**. Isaac no captura el % directo: captura el monto que entregó y el monto neto que realmente se movió (ej. pesos que dio vs. dólares que confirmó el agente que mandó), y el sistema calcula solo el % — ese % calculado se guarda como el fee de referencia de ese proveedor financiero para la próxima vez. Si se llena, se crea automático un gasto aparte en categoría "Comisiones", ligado a ese movimiento (una sola captura, mismo mecanismo del punto 1 — nunca dos registros sueltos).
   - **¿La comisión se mete al costo del producto o se queda como gasto?** Depende de si el movimiento está ligado a un contenedor: si es un pago a China o un pago de crédito de aduana, la comisión se SUMA al costo de ese contenedor (junto con flete/aduana/otros gastos) — es un costo directo de traer la mercancía, igual que el flete. Si el movimiento no está ligado a ningún contenedor (pagar a un proveedor de México, comisión al recibir dinero de ventas), es un gasto normal de Finanzas que no toca el costo de ningún producto.
   - **El préstamo (o su interés, cuando lo tenga) nunca se mete al costo del producto** — es un gasto financiero aparte (de cómo se consiguió el dinero, no de traer la mercancía), aunque ese dinero se haya usado para pagar a China. Solo la comisión de la transferencia en sí se absorbe al costo, no el préstamo.
9. **Pagos a China = pagos_mercancia, extendido**: la tabla de abonos de mercancía que ya existe (Módulo 1) se le agregan campos de Finanzas (cuenta de la que salió, proveedor financiero, comisión vía el mecanismo del punto 8). Al marcar un abono como "Pagado", se genera su movimiento de salida en Finanzas (y el de la comisión, si aplica) automáticamente — sigue siendo la misma pantalla de Abonos del contenedor que Isaac ya conoce, no una nueva.
10. **Crédito de contenedor, es una opción dentro de cada contenedor (NUEVO, pendiente de construir)** — los dos casos reales de Isaac:
    - **Crédito del proveedor chino, en dólares**: la fábrica le da crédito, lo va abonando cuando puede. Reutiliza el mismo mecanismo que "Abonos de mercancía" (que ya soporta marcar un abono como "Pendiente", no solo "Pagado" — solo le falta la fecha límite).
    - **Crédito del agente aduanal, en pesos, con fecha a 60 días típicamente**: el agente aduanal paga el fideicomiso de aduanas por adelantado, e Isaac se lo debe pagar después (ej. a 60 días) — es crédito nuevo, no existe hoy (`aduana_pesos` en el contenedor es solo un total, sin abonos ni fecha de pago). Se construye con el mismo patrón que mercancía (lista de abonos, cada uno "Pagado"/"Pendiente" con su fecha límite), pero en pesos y sin tipo de cambio.
    Ambos son opcionales por contenedor (no todo contenedor tiene crédito) y ambos se reflejan en Finanzas como deuda viva, separado por moneda: "debo en pesos" (préstamos en pesos + crédito de aduana pendiente) y "debo en dólares" (préstamos en dólares + crédito de proveedores pendiente).
11. **Dashboard financiero** (`/finanzas`): saldo por cuenta, gasto por categoría, comisiones pagadas acumuladas, sueldo retirado acumulado, deuda viva (separada en pesos y dólares), y cuánto dinero se ha mandado por contenedor. El detalle línea por línea de la deuda vive en `/finanzas/deudas` (punto 6).
12. **`/finanzas` (panel principal) es primero que nada el registro, no un resumen pasivo** — así lo pidió Isaac, como cualquier app de finanzas seria: arriba hay 3 botones — "Agregar dinero" (entrada, ej. recibir efectivo o depósito en banco), "Mandar dinero" (salida — NO se llama "gastar", Isaac lo aclaró: son pagos/transferencias que manda, no necesariamente un gasto propio) y "Mover entre mis cuentas" (transferencia). Abajo, saldos por cuenta y los últimos movimientos, con el historial completo en `/finanzas/movimientos`.
13. **La comisión opcional aplica a salidas Y a transferencias** — Isaac aclaró que a veces mover dinero entre sus propias cuentas también genera comisión (ej. cargo del banco). Mismo mecanismo en ambos casos: el monto neto (lo que de verdad le llega al destinatario o a la cuenta destino) se guarda en el movimiento principal, y la diferencia se separa sola como gasto en "Comisiones" desde la misma cuenta de origen — nunca se infla ni se duplica. (Para entradas, el tema de comisiones se resuelve cuando se conecte ventas — Isaac ya lo sabe.)
14. **Facturas pendientes de pagar** (`facturas_pendientes` + `pagos_factura`, migración 0023, pantalla `/finanzas/facturas`): seguimiento de cuentas por pagar sueltas (proveedores de México, servicios, etc.) — completamente independiente de préstamos, deuda de proveedores de China o crédito de contenedor, nunca toca esas tablas. Isaac captura folio (opcional), proveedor, concepto, monto total con IVA incluido, moneda, fecha de la factura y fecha límite opcional. Ya NO se marca "Pagada" de un jalón: se le van registrando **pagos parciales** ("Registrar pago de factura" — elige la factura, monto, cuenta y fecha), cada uno genera su salida real en Finanzas en la misma operación (mismo mecanismo de "una sola captura"). El saldo de la factura NUNCA se guarda a mano: se calcula (monto − suma de sus pagos, `src/lib/calculos-facturas.ts`) — pasa a "Pagada" solo cuando el saldo llega a cero. Cada pago se puede editar (corrige también su movimiento de Finanzas ligado) o quitar (borra su movimiento con él). Solo se puede borrar una factura que todavía no tiene ningún pago registrado. **Pago "sin cuenta" para historial de antes del sistema** (migración 0025, `cuenta_id` opcional en `pagos_factura`): al EDITAR un pago ya guardado, la cuenta se puede dejar en "Sin cuenta (fue antes de usar el sistema)" — sirve para un pago que de verdad ya había salido de una cuenta (ej. BBVA) mucho antes de que Isaac capturara su saldo inicial en el sistema; borra el movimiento de Finanzas ligado (ya no resta otra vez de esa cuenta, sería contarlo doble) pero deja el abono guardado en la factura para que su saldo siga cuadrando. Si después se le vuelve a poner una cuenta, se crea el movimiento de Finanzas de cero. Mismo espíritu que `cuenta_id`/`movimiento_financiero_id` opcionales en socios/prestamistas/deuda de proveedores (punto 16).
15. **Maaser (diezmo)** (`/finanzas/maaser`, migración 0021): Isaac aparta el 10% de su ganancia neta. Como todavía no hay forma de calcular la ganancia sola (eso llega con ventas conectadas a Mercado Libre, Módulo 4), él registra su ganancia neta a mano cada vez que la calcula (`registros_ganancia` — solo informativo, no mueve dinero de ninguna cuenta). Los pagos de Maaser NO tienen pantalla ni captura aparte: se registran como cualquier salida normal en "Movimientos" eligiendo la categoría fija "Maaser" (mismo patrón que "Sueldo"). La pantalla de Maaser (una pestaña más dentro de Finanzas, no en el menú raíz) suma las ganancias registradas, calcula el 10%, suma lo ya pagado (categoría "Maaser"), y muestra el balance: a favor (dio de más) o cuánto debe todavía.

16. **Socios, Prestamistas y Deuda con proveedores** (migración 0022): construidos a partir del Excel real de Isaac ("Finanzas_Daymart") — mismo patrón de un solo libro por catálogo que Maaser (`registros_ganancia`):
    - `socios` + `movimientos_socio` (tipo APORTE/REPARTO) — saldo = aportado − repartido, por moneda. `/finanzas/socios`.
    - `prestamistas` + `movimientos_prestamista` (tipo PRESTAMO/PAGO) — saldo = prestado − pagado, por moneda. `/finanzas/prestamistas`.
    - `movimientos_deuda_proveedor` (proveedor en texto libre, tipo CARGO/ABONO) — la deuda es por PROVEEDOR en general, no por contenedor específico (un abono se aplica "a cuenta" del proveedor, no a un pedido exacto). `/finanzas/proveedores`.
    - Cada movimiento nuevo capturado desde la pantalla exige cuenta y genera su movimiento real en Finanzas en la misma operación (categorías fijas: "Aporte a capital", "Repartos", "Préstamo recibido", "Pago préstamo", "Pago proveedor") — mismo principio de "una sola captura" del resto del módulo. `cuenta_id` y `movimiento_financiero_id` son opcionales en las tres tablas SOLO para poder cargar historial de antes del sistema (donde no siempre se sabe a qué cuenta exacta entró/salió el dinero) sin inventar una cuenta falsa — la carga histórica del Excel de Isaac se hizo así, sin generar movimientos de Finanzas ni tocar el saldo de ninguna cuenta.
    - `/finanzas/balance`: activo (saldo de cuentas + valor de inventario) contra pasivo (préstamos vivos + deuda con proveedores) — pendiente agregarle la mercancía ya pagada pendiente en China.
    - Categorías reales de Isaac (su lista completa de "Listas Conceptos" del Excel) ya están cargadas en `categorias_financieras`.

17. **Cuenta puente + vencimientos por cargo** (migración 0024) — a partir del flujo real que Isaac explicó (estado de cuenta de su encargado financiero "Jaim T.", factura real de Optiker):
    - **Cuentas "de tránsito"** (`cuentas_financieras.cuenta_transito`): para cuentas como Mercado Pago donde Isaac NO lleva saldo real (solo quiere etiquetar de dónde salió un gasto) — se excluyen de la suma de "Activo" en `/finanzas/balance`, en el total de `/finanzas`, y del "Saldo en Finanzas" del dashboard de inicio, pero se siguen viendo normal en Cuentas/Movimientos.
    - **Pagar una factura "a través de una cuenta puente"**: en `/finanzas/facturas`, "Registrar pago de factura" ahora tiene dos modos — "Pago directo" (sale de una cuenta y ya) o "A través de una cuenta puente" (genera una TRANSFERENCIA hacia esa cuenta puente, con comisión opcional igual que cualquier transferencia) — el monto que se abona a la factura es el bruto, aunque a la cuenta puente solo le llegue el neto.
    - **Enviar desde una cuenta puente** (`registrarEnvioCuentaPuente`, sección nueva en `/finanzas/proveedores`): cada envío de la cuenta puente hacia un proveedor (ej. mandarle dólares a un proveedor chino a través del encargado financiero) trae SU PROPIO tipo de cambio + comisión — en una sola captura: sale la salida real de la cuenta puente en Finanzas, se abona a la deuda de ese proveedor (en su propia moneda), y si se liga a un contenedor, se registra el abono de mercancía (`pagos_mercancia`, ahora con `cuenta_id`/`movimiento_financiero_id`) con el tipo de cambio EFECTIVO de esa transacción (pesos + comisión ÷ dólares) — la comisión se absorbe al costo de la mercancía, igual que flete/aduana. Al borrar ese abono (`eliminarAbono`), se borra también su movimiento de Finanzas y su abono a la deuda del proveedor ligados.
    - **Vencimientos por cargo, no por proveedor**: cada cargo de deuda con un proveedor puede traer su propia fecha límite (`movimientos_deuda_proveedor.fecha_limite`) — un proveedor puede tener varios cargos con fechas distintas. Los abonos se aplican al cargo más viejo primero (FIFO, `cargosAbiertosProveedor` en `src/lib/calculos-socios-deuda.ts`) para saber cuál sigue abierto. Arriba de todas las pantallas de Finanzas aparece un aviso (`cargosPorVencer`) cuando algún cargo vence en 7 días o menos, o ya venció.
    - Categoría nueva: "Gastos Mercado Libre (publicidad, almacenamiento, envíos)" — el cargo mensual que hace Mercado Libre (aparte de sus comisiones), pagado desde Mercado Pago, se registra como salida normal desde esa cuenta de tránsito.

18. **Editar/quitar un movimiento "suelto" en `/finanzas/movimientos`** (`actualizarMovimiento`/`eliminarMovimiento`): pensado para el caso real de Isaac — saca un sueldo y luego el mismo día quiere sumarle un complemento, así que entra y corrige el monto en vez de crear un movimiento aparte. Solo se puede editar/quitar un movimiento que NO tenga dueño en otra pantalla — se revisa que su id no aparezca en `pagos_mercancia.movimiento_financiero_id`, `movimientos_deuda_proveedor.movimiento_financiero_id` ni `pagos_factura.movimiento_financiero_id`; si sí aparece (viene de un abono de contenedor, de un abono a proveedor o de un pago de factura), no se deja tocar aquí — sale sin los links de "editar/quitar" y hay que corregirlo desde su pantalla de origen, para no desincronizar los dos registros del mismo dato (mismo principio de "una sola captura" de todo el módulo). Se puede corregir: monto, fecha, cuenta (o cuenta destino si es transferencia), categoría, contraparte y notas — el tipo (Entrada/Salida/Transferencia) y la moneda no se cambian, para no volver ambiguo qué significa el movimiento.

20. **Comisión por porcentaje y "a cuenta de una factura" desde el registro de movimientos** (18 sep; caso real: Isaac manda $107,000 de BBVA a la cuenta de su encargado financiero "Jaim T." con 1.75% de comisión, y ese pago va a cuenta de una factura fiscal). En `FormularioMovimiento` (Resumen), para Salida y Transferencia:
    - **Comisión de tres formas**: Porcentaje (ej. 1.75), Monto de la comisión, o Neto que llegó. El monto es controlado y se muestra en vivo "se debitan X: llegan Y y Z se guardan en Comisiones". Al servidor siempre viaja `monto_neto` (monto − comisión), igual que antes: no cambió `registrarMovimiento`.
    - **"¿Es a cuenta de una factura?"**: selector con las facturas con saldo (`facturasAbiertas` en `finanzas/page.tsx`). Si se elige, el formulario llama a `registrarPagoFactura` (una sola captura: movimiento + `pagos_factura` ligados) en vez de `registrarMovimiento`; `registrarPagoFactura` ahora acepta comisión también en pagos directos (antes solo vía cuenta puente) y `monto_factura` opcional = cuánto se descuenta de la factura (por defecto el monto completo; desde el formulario, si hay comisión, se manda el neto que llega, editable).
    - **"La comisión la sé después" en CUALQUIER salida o transferencia** (migración 0036: `movimientos_financieros.comision_pendiente`): cuarta opción en el bloque de comisión. El movimiento se guarda por el monto completo y marcado; sale la pastilla ámbar "falta comisión" en Movimientos y en el estado de cuenta, y el aviso ámbar en el layout de Finanzas y en Resumen (`PendientesChina`, que lista envíos a China Y movimientos marcados). Al ponerla (`completarComisionMovimiento`: %, $ o neto) el movimiento baja al neto, la comisión se inserta aparte en "Comisiones" (`referencia_tipo COMISION`), y si era pago de factura el abono a la factura baja al neto.
    - **Botón "Pagar a proveedores"** (Isaac lo renombró así: "no es nada más mandar dinero a China"; aprobado con "va", migración 0036: tabla `envios_china`; cuarta pestaña en `FormularioMovimiento` → `formulario-envio-china.tsx` → `mandarDineroChina`): de qué cuenta sale, a través de quién (Intermediario = cuenta puente, o Directo), pesos, proveedor (sugerencias = proveedores con deuda + fábricas de contenedores), moneda de su deuda, contenedor y abono pendiente opcionales, dólares que llegan, comisión (La sé después / % / $ / neto), fecha, a cuenta de factura opcional. Vía intermediario: hoy se inserta la TRANSFERENCIA origen → puente por el monto COMPLETO (sin comisión: la comisión de un pago a China se absorbe al costo, no es gasto) + `envios_china` PENDIENTE; si la comisión ya se sabe, `completarEnvioChinaInterno` llama de inmediato a `registrarEnvioCuentaPuente` (que ahora devuelve `movimientoId`) desde la cuenta puente con pesos netos + comisión + dólares → abono a la deuda del proveedor + abono del contenedor con tipo de cambio efectivo; si no, queda el aviso ámbar y `completarEnvioChina` lo cierra cuando llega el recibo. Directo = requiere la comisión (o 0) y se registra completo desde la cuenta de origen. `cancelarEnvioChina` (solo pendiente) borra la transferencia y su pago de factura ligado. La transferencia de un envío pendiente cuenta como "ligada" (`movimientoEstaLigado`, listado de Movimientos, estado de cuenta) para que no se edite por fuera. Flujo real de Isaac: BBVA → Jaim T. $107,000, comisión 1.75% (la sabe al día siguiente con el recibo), a cuenta de la factura fiscal de Senado; el saldo de la cuenta Jaim T. es su saldo a favor/en contra con Jaime (se cuadra con "Ajustar saldo"). NUNCA registrar además un "Pago a cuenta" del mismo dinero.
25. **Categorías con buscador + crear al vuelo + filtro en Movimientos** (23 sep; Isaac: "el desplegable de categorías es enorme"). `src/app/finanzas/selector-categoria.tsx` (`SelectorCategoria`): campo de texto que filtra las categorías ordenadas por abecedario (`localeCompare` es), opción "Sin categoría", y si lo escrito no existe, "+ Crear categoría «X»" — viaja como `categoria_nueva` y `resolverCategoriaId` (actions.ts) la da de alta (o reutiliza por nombre, `ilike`) al guardar en `registrarMovimiento`, `actualizarMovimiento` y `registrarPagoFactura`. Reemplazó al `Selector` de categoría en `formulario-movimiento.tsx`, `facturas/registrar-pago.tsx` y `movimientos/formulario-editar-movimiento.tsx`. En `/finanzas/movimientos`, `FiltroCategoria` (mismo componente con `permitirCrear=false` y `onChange` → `?categoria=<id>`, "sin" = sin categoría) filtra el libro y muestra cuántos movimientos y el neto (entradas − salidas, sin transferencias) de esa categoría.
23. **Libro de Movimientos como lista, no tabla** (22 sep; Isaac: "que se vea más bonito y acomodado", también en celular). `movimientos/page.tsx` agrupa por mes (encabezado "septiembre de 2026") y `fila-movimiento.tsx` es un `<li>`: día/mes en bloque a la izquierda (calendario CDMX vía `fechaTextoMx`), concepto (contraparte en negritas + pastilla Entrada/Salida/Transferencia, notas en chiquito, línea gris con cuenta → destino · categoría), monto grande a la derecha con editar/quitar debajo. Sin scroll horizontal en celular. Los movimientos ligados ya no muestran "—" mudo: dicen "viene de un pago de factura, se corrige desde ahí" (`origenLigado`, mismo mapa que el estado de cuenta). **La comisión pendiente se pone en la misma fila**: la pastilla ámbar "falta comisión · ponerla" abre `FormularioComision` (`src/app/finanzas/formulario-comision.tsx`, extraído de `pendientes-china.tsx` para usarlo en los dos lados) → `completarComisionMovimiento`. Isaac creía que "falta comisión" lo mandaba a registrar una comisión nueva suelta: no, es la MISMA transacción (baja al neto y la comisión queda ligada con `referencia_id`), solo que antes el link lo llevaba al aviso ámbar de Resumen. Un movimiento ligado (ej. transferencia de un pago de factura) sigue sin editar/quitar aquí, pero SÍ se le puede poner la comisión desde su fila.
24. **Panel de publicaciones de ML (`/mercadolibre/stock`) como tarjetas** (22 sep; Isaac: "se ve muy amontonado, no se ven claros los MLM"): cada producto es un `<li>` con foto grande (20–24), pastillas de estado/logística/catálogo, título a dos líneas (`line-clamp-2`), variante, y los IDs en pastillas monoespaciadas (`item_id` gris; las publicaciones que comparten stock en violeta); los números (Valor / Precio / En Full / Publicado) en su propia rejilla gris de 4 columnas, y "Producto del CRM" en su bloque a la derecha (en celular se apilan los tres bloques). `LigaProducto` en modo edición ocupa todo el ancho en celular.
22. **Pago "sin cuenta" también al REGISTRAR** (22 sep; caso real: Isaac le pagó $200,000 a Senado unos días antes de empezar a usar el sistema y la deuda seguía en $985,000). En Finanzas → Proveedores → "Pagar a cuenta" y en Facturas → "Registrar pago de factura" (solo modo "Pago directo", sin comisión), el selector de cuenta tiene la opción "Sin cuenta (fue antes de usar el sistema)": `registrarAbonoProveedor` / `registrarPagoFactura` insertan solo el ABONO / `pagos_factura` con `cuenta_id` y `movimiento_financiero_id` en null, sin movimiento de Finanzas — baja la deuda/saldo pero no resta de ninguna cuenta real (antes solo se podía llegar a eso editando un pago de factura ya guardado, y en Proveedores no había forma). Recomendación dada a Isaac: registrar el pago histórico así, en vez de editar el cargo original, para que el historial diga "debía 985,000 y pagué 200,000".
21. **Tarjeta "Reinvertido en mercancía" en Resumen** (20 sep, aprobado con "Va"): junto a "Total en pesos / Total en dólares" hay una tercera tarjeta verde con la suma de los ABONOS de `movimientos_deuda_proveedor` (lo que ya se les pagó a los proveedores, por moneda: dólares grande, pesos abajo si hay). Isaac quería ver de un vistazo cuánto del dinero que entró (préstamos) ya está trabajando en China. Se calcula en `finanzas/page.tsx` con `deudaProveedores`, que ya se cargaba ahí.
19. **Estado de cuenta por cuenta** (`/finanzas/cuentas/[id]`, migración 0034; Isaac: "le pico a Banco BBVA y veo sus movimientos, lo filtro por mes, lo imprimo y se lo mando al contador"). Aprobado con "va".
    - **Cuentas (`/finanzas/cuentas`) es solo la lista**: cada cuenta es un link a su estado de cuenta (saldo, tipo, número de movimientos). Isaac NO quiso el "Quitar" rojo a la vista: alta y baja viven en un modo **"Editar cuentas"** (link chiquito abajo, `editar-cuentas.tsx`); quitar exige escribir el nombre exacto (`eliminarCuenta(cuentaId, formData)` valida `confirmacion`) y sigue siendo soft delete: los movimientos se quedan y siguen contando.
    - **Cálculo** en `src/lib/estado-cuenta.ts` (`estadoDeCuenta`): saldo inicial = suma de lo anterior al periodo, cada renglón con entrada/salida y saldo corrido, totales y saldo final; una transferencia sale como "Transferencia a X" (salida) en la cuenta origen y "Transferencia desde X" (entrada) en la destino. Fechas comparadas en calendario CDMX (`fechaTextoMx`). `periodoDeParams`: `?mes=AAAA-MM` (por defecto el mes actual, corta en día 1) o `?desde&hasta`; `?moneda=USD` cambia de pesos a dólares si la cuenta tiene movimientos en USD. `datos.ts` (`cargarEstadoCuenta`) alimenta las tres salidas para que digan lo mismo: pantalla, **Imprimir / PDF** (`/imprimir`, Logo + tabla limpia, `BotonImprimir`) y **Descargar Excel** (`/exportar` → CSV con BOM, se abre en Excel).
    - **Editar ahí mismo** con el mismo formulario del libro de Movimientos, ahora compartido (`movimientos/formulario-editar-movimiento.tsx`): al editar pregunta **"¿Qué es este movimiento?" Entrada / Salida / Ajuste** (`tipo_edicion`; Ajuste pide "Suma al saldo / Resta del saldo" = `ajuste_signo`). `actualizarMovimiento` SÍ cambia ahora el tipo entre ENTRADA y SALIDA (antes no se podía); Ajuste = ENTRADA o SALIDA con la categoría fija **"Ajuste de saldo"** (`CATEGORIA_AJUSTE`, la crea `categoriaAjusteId` si falta el SQL 0034). Una TRANSFERENCIA sigue sin cambiar de tipo. Los movimientos ligados (abono de contenedor, Proveedores, pago de factura, cobro de venta) se ven pero sin editar/quitar, con la nota "viene de … — se corrige desde ahí" (`origenLigado`).
    - **"¿No cuadra con el banco? Ajustar saldo"** (`ajustar-saldo.tsx` → `ajustarSaldoCuenta`): Isaac escribe el saldo real, el sistema calcula la diferencia contra `saldoCuenta` y registra UN movimiento "Ajuste de saldo" (entrada o salida) con esa diferencia; muestra en vivo qué va a registrar.
    - **"+ Movimiento en esta cuenta"** → `/finanzas?cuenta=<id>`; `FormularioMovimiento` acepta `cuentaInicial` y la deja preseleccionada.
    - `revalidarFinanzas()` centraliza los `revalidatePath` de Finanzas (incluye `/finanzas/cuentas`).
    - Pendiente por decidir con Isaac: si también quiere un reporte consolidado de todas las cuentas (por ahora es uno por cuenta) — no contestó esa pregunta.

## Módulo 4: Ventas directas (a clientes, fuera de Mercado Libre)

Solo dueño (`/ventas`, ítem "Ventas" en el menú ☰ y acceso rápido en el dashboard; la operadora queda fuera por `RUTAS_OPERADORA` del proxy). Isaac lo pidió para vender directo a un cliente (de contado o a crédito) sin registrarlo como salida a Mercado Libre. Plan aprobado con "Va"; sus respuestas: solo él lo usa por ahora, casilla opcional de IVA, nota de venta imprimible, y pagos parciales permitidos. Migración 0028.

- **Tablas**: `clientes` (nombre, teléfono, notas, `dias_credito` habituales, soft delete), `ventas` (`numero` corrido automático, cliente, bodega, fecha, `forma_pago` CONTADO|CREDITO, `con_iva`, `fecha_limite` solo a crédito, notas), `venta_lineas` (sku, nombre, foto, cantidad, `precio_unitario` SIN IVA, `costo_unitario` = foto del costo promedio del SKU al momento de vender, para el margen), `cobros_venta` (monto, fecha, cuenta, notas, `movimiento_financiero_id`). `movimientos_stock.venta_id` liga cada salida de stock a su venta. Categoría fija nueva en Finanzas: **"Ventas directas"**.
- **Nada se guarda a mano** (`src/lib/calculos-ventas.ts`): subtotal = Σ líneas; IVA = 16% del subtotal solo si `con_iva`; total; cobrado = Σ cobros; saldo = total − cobrado; margen bruto = subtotal (sin IVA) − costo. Una venta está "pagada" cuando el saldo ≤ $0.01.
- **`registrarVenta`** (`src/app/ventas/actions.ts`) hace TODO en una sola operación: crea la venta y sus líneas, genera las SALIDAS de stock (`insertarMovimientosStock`, destino "Venta directa", referencia "Venta #N — cliente", `venta_id`), y si es de contado registra el cobro por el total con su ENTRADA real en Finanzas. Valida que haya stock suficiente por SKU (no se vende lo que no hay). Si algo falla a medio camino, `deshacer()` borra lo ya guardado — nunca queda una venta "a medias". **De contado = venta a crédito con un solo cobro automático por el total**: así todas las ventas usan el mismo libro de cobros y no hay dos caminos distintos.
- **Cobros parciales** (`registrarCobro`/`actualizarCobro`/`eliminarCobro`, `[id]/cobros.tsx`): cada cobro genera su ENTRADA en Finanzas (categoría "Ventas directas", contraparte = cliente, `referencia_tipo` "VENTA"); editar corrige el movimiento ligado y quitar lo borra — mismo mecanismo de "una sola captura" de Finanzas. No se puede cobrar más del saldo. En `/finanzas/movimientos`, esos movimientos aparecen bloqueados (sin editar/quitar) igual que los de abonos/facturas (`movimientoEstaLigado` ya revisa `cobros_venta`).
- **Cancelar venta** (`eliminarVenta`, botón en el detalle, se confirma escribiendo el número de la venta): borra sus salidas de stock (la mercancía "regresa"), los movimientos de Finanzas de sus cobros, y la venta con líneas y cobros.
- **Pantallas**: `/ventas` (lista con total vendido y "te deben", + Nueva venta), `/ventas/nueva` (formulario: cliente existente o "+ Cliente nuevo" en línea, fecha, productos con `SelectorProducto` — solo SKUs con stock > 0, muestra stock y costo, propone el ÚLTIMO precio al que se vendió ese SKU —, casilla "Agregar IVA (16%)", contado con cuenta destino / crédito con fecha límite propuesta = fecha + `dias_credito` del cliente o 30, bodega, totales y ganancia estimada en vivo), `/ventas/[id]` (detalle con tarjetas total / te debe / ganancia bruta, líneas con costo, cobros), `/ventas/[id]/imprimir` (nota de venta con Logo, sin costos ni márgenes, "Imprimir → Guardar como PDF"), `/ventas/por-cobrar` (todo con saldo, fecha límite más próxima primero, total vencido, resumen por cliente), `/ventas/clientes` (alta, edición en línea y baja; no se puede quitar un cliente que todavía debe).
- **Avisos y balance**: el layout de Ventas y el de Finanzas muestran un aviso (azul en Finanzas, para distinguirlo del ámbar de "pagos que debes") con las ventas a crédito que vencen en 7 días o ya vencieron (`ventasPorVencer`). En `/finanzas/balance`, "Te deben clientes (ventas a crédito)" se suma al activo.
- **IVA — interpretación elegida**: los precios se capturan SIN IVA y la casilla lo agrega encima. Si Isaac prefiere capturar precios que ya incluyen IVA (y que el sistema lo desglose), se cambia el cálculo, no la estructura.

## Módulo 5 (en construcción): Mercado Libre

Isaac quiere conectar su cuenta de vendedor para ver ventas, stock en Full y automatizar comisiones, todo en **horario de la Ciudad de México** (`src/lib/fechas-mx.ts`, `ZONA_MX`). Pidió construir las pantallas grandes aparte (vista previa) — pero la **conexión** sí vive en `main`, porque Mercado Libre regresa a un link fijo registrado: `https://daymart-crm.vercel.app/api/mercadolibre/callback` (el CRM real vive en `https://daymart-crm.vercel.app`).

- **Aplicación en Mercado Libre** (la creó Isaac en developers.mercadolibre.com.mx/devcenter con su cuenta de vendedor): App ID `8513273452596590` (no es secreto). Secret Key SOLO en Vercel, nunca en el chat ni en el repo. Variables de entorno: `MERCADOLIBRE_APP_ID`, `MERCADOLIBRE_SECRET_KEY`, `MERCADOLIBRE_REDIRECT_URI`. Flujos: Authorization Code + Refresh Token, sin PKCE. Notificaciones a `https://daymart-crm.vercel.app/api/mercadolibre/notificaciones` (tópicos orders_v2, items, shipments).
- **Migración 0029**: `mercadolibre_conexion` (una sola fila, id=1: ml_user_id, nickname, access_token, refresh_token, expira_en, scope) y `mercadolibre_notificaciones` (topic, resource, payload, recibido_en, procesado_en). **RLS activado SIN políticas a propósito**: nadie las lee desde el navegador; solo el servidor con la service role key (`src/lib/supabase/servicio.ts`, `createServiceClient`). Las llaves equivalen a la contraseña de Mercado Libre de Isaac.
- **OAuth** (`src/lib/mercadolibre-auth.ts`): `/api/mercadolibre/conectar` (solo dueño; genera `state` al azar en cookie httpOnly y manda a auth.mercadolibre.com.mx) → `/api/mercadolibre/callback` (verifica `state`, cambia el código por tokens en `api.mercadolibre.com/oauth/token`, consulta `/users/me` para el nickname, guarda; redirige a `/mercadolibre?ok=1` o `?error=`). `obtenerAccessToken()` renueva solo con el refresh token cuando faltan < 5 min para vencer (el access token dura 6 h; el refresh token es de UN solo uso, cada renovación trae uno nuevo que se guarda). `mercadolibreGet(ruta)` es la llamada genérica con token vigente.
- **Webhook** `/api/mercadolibre/notificaciones` (POST, pública en `RUTAS_PUBLICAS` del proxy): solo guarda el aviso y contesta 200 en seguida (Mercado Libre exige < 500 ms); rechaza avisos de otra `application_id`. El procesamiento real (jalar la orden, etc.) viene en la Fase 1.
- **Pantalla de Conexión `/mercadolibre/conexion`** (solo dueño; Isaac pidió que la ENTRADA de Mercado Libre sea Ventas, no la conexión — la conexión quedó como pestaña secundaria y como link discreto "conexión" bajo el botón Sincronizar): estado de conexión (nickname, conectado desde, vigencia del token), botón "Conectar mi cuenta" (link a `/api/mercadolibre/conectar`), "Probar conexión" (llama `/users/me` de verdad), "Desconectar", y la lista de los últimos avisos recibidos para verificar que el webhook llega.
- **Fase 1 — Ventas de ML (`/mercadolibre`, la pantalla principal; `/mercadolibre/ventas` solo redirige; migración 0030)**: copia local de las órdenes en `mercadolibre_ordenes` + `mercadolibre_orden_items` + `mercadolibre_sync` (RLS sin políticas, lectura solo por servidor). `src/lib/mercadolibre-ordenes.ts`: `sincronizarOrdenes()` pagina `/orders/search?seller=…&order.date_created.from=…` (60 días la primera vez; después desde la última sync − 3 días; botón "traer último año" = 365), guarda cada orden con `guardarOrden()` (upsert + reemplaza items; foto del item vía `/items/{id}?attributes=thumbnail,pictures` con caché por corrida). Datos de envío (`/shipments/{id}` → logística: fulfillment=Full, cross_docking=Colecta, drop_off=Punto de envío, self_service=Flex; `/shipments/{id}/costs` → `senders[].cost` = lo que paga Isaac) solo se piden para órdenes que no los tienen, máx. 120 por corrida. **Comisión verificada sola** (`comisionRenglon`): ML reporta `sale_fee` por unidad, pero como Isaac no puede comprobarlo y la documentación no se pudo consultar desde este entorno (egress bloqueado), para renglones de > 1 pieza se compara `sale_fee` contra la tarifa oficial `/sites/MLM/listing_prices?price=<precio unitario>&category_id=&listing_type_id=` (con caché por corrida, tolerancia 15%): si se parece a la tarifa de UNA pieza se multiplica por la cantidad; si ya se parece a tarifa × cantidad se deja; si no hay tarifa, regla por defecto = por unidad × cantidad. El resultado se guarda por renglón en `mercadolibre_orden_items.comision` (migración 0031) y la orden suma esos renglones. `procesarNotificacionesPendientes()` atiende los avisos del webhook (orders_v2 → jala esa orden; shipments → refresca costo/logística; items → solo se marca visto) y corre al abrir la pantalla de Ventas y al sincronizar. Pantalla: filtro **Hoy / Esta semana (desde el lunes) / Este mes / Elegir fechas** (`filtro-fechas.tsx`: rango desde–hasta con el `CampoFecha` propio, `?desde=AAAA-MM-DD&hasta=AAAA-MM-DD`; límites calculados en CDMX con `inicioDelDiaMx`/`inicioDeSemanaMx`/`inicioDelMesMx`/`instanteDesdeFechaMx`, `hasta` exclusivo = 00:00 del día siguiente). **Auto-sync al abrir**: si la última sincronización tiene > 10 min, se refrescan los últimos 2 días antes de mostrar (así las ventas nuevas aparecen sin picarle). Tarjetas (ventas pagadas — con "N compras" si hay carritos con `pack_id` compartido, porque ML cuenta un carrito como una venta —, piezas, total, comisiones, envíos a cargo, "te queda") + línea "Además: X pendientes de pago y Y canceladas, no entran en los totales" para explicar diferencias contra el panel de ML, **tabla principal "Por producto"** (Isaac no quiere listas infinitas de ventas): ventas pagadas del periodo consolidadas por `item_id` + `variation_id` (cada variante es un producto aparte; `mercadolibre_orden_items.variacion` guarda "Color: Negro · Talla: M" desde `variation_attributes`, migración 0031) con foto, título, variante/SKU, piezas, número de ventas, facturado, precio promedio (facturado ÷ piezas) y comisión, ordenado por facturado; el detalle de órdenes una por una (fecha+hora CDMX, comprador, total, comisión, envío "…" hasta que ML publique el costo, logística, estado `ESTADOS_ORDEN`) queda colapsado en un `<details>` "Ver las N ventas una por una". Solo lectura: NO toca stock ni Finanzas (por eso se publicó directo en `main`).
- **Horario verificado**: `src/lib/fechas-mx.ts` — las fechas de ML llegan con offset (ej. `-04:00`), se guardan como instante (`timestamptz`) y se muestran con `timeZone: America/Mexico_City`; `inicioDelDiaMx`/`inicioDelMesMx` calculan el desfase con `Intl` (no a mano, por si México vuelve a cambiar de horario). Probado: `2026-09-16T01:30-04:00` → "15 sep 2026, 11:30 p.m." CDMX; inicio de día/mes correctos en la frontera de medianoche. En Conexión hay un reloj en vivo CDMX (`reloj-mx.tsx`) para que Isaac compare contra ML.
- **Fase 3 — Comisión real en Research (`src/lib/mercadolibre-simulador.ts`)**: Isaac NO quiso una pantalla de simulador aparte ("no lo quiero doble"): se quitó `/mercadolibre/simulador` y la pestaña. En Research, al darle **"Traer datos"** (`alTraerDatos`), además de foto/nombre/precio/categoría se consulta la comisión real con `porcentajeComisionMl` (acción en `src/app/mercadolibre/actions.ts` → `comisionesMercadoLibre(precio, categoriaId)` → `/sites/MLM/listing_prices?price=&category_id=`) y se llena sola "Comisión de Mercado Libre (%)", usando el mismo tipo de publicación que el anuncio de referencia (`tipoPublicacion` = `listing_type_id` del item: gold_pro → Premium, si no → Clásica). Debajo del campo quedan dos pastillas "Clásica | Premium" para recalcular con el otro tipo (o después de cambiar el precio de venta). El costo de envío en Research sigue saliendo de la tabla oficial guardada (`costos-envio-ml.ts`).
- **Fase 2 — Stock en Full (`/mercadolibre/stock`, migración 0032, `src/lib/mercadolibre-stock.ts`)**: `sincronizarPublicaciones()` trae TODOS los IDs de publicaciones (`/users/{id}/items/search?search_type=scan` con `scroll_id`), luego `/items?ids=…&attributes=…` en lotes de 20; cada variante es un renglón en `mercadolibre_publicaciones` (título, variante, foto de la variante, precio, estado, logística, `seller_sku` = `seller_custom_field` o atributo `SELLER_SKU`, `catalogo` = `catalog_listing`, `relacion_item_id` = `item_relations[0].id`, `inventory_id`, `cantidad_publicada` = `available_quantity`, y `full_disponible`/`full_no_disponible`/`full_detalle` de `/inventories/{inventory_id}/stock/fulfillment` con caché por corrida). La tabla se REEMPLAZA completa en cada sync. **Regla de catálogo (investigada y confirmada con Isaac)**: una publicación de catálogo comparte stock y SKU con su publicación tradicional y en Full ambas traen el MISMO `inventory_id` → `resumenFull()` cuenta cada `inventory_id` una sola vez (dos publicaciones Full con inventarios distintos SÍ se suman: 1+3 y 1+7 = 10, como explicó Isaac); en pantalla el par se marca con la pastilla "catálogo" y la nota "mismo inventario que MLM…". **Liga con el CRM** (`mercadolibre_vinculos`, sobrevive a las re-sincronizaciones): `skuCrmDe()` usa primero la liga manual y, si no hay, la automática cuando `seller_sku` coincide con un SKU del CRM (hoy los SKUs de ML NO coinciden; Isaac los va a unificar después). Sin liga → botón "Ligar con producto" (`liga-producto.tsx`, reutiliza `SelectorProducto` con foto); `vincularPublicacion`/`desvincularPublicacion`. **Valor en Full** = piezas disponibles × costo promedio del SKU ligado (sin liga = sin valor, se avisa "N sin ligar"). Pantalla: tarjetas En bodega (CRM) / En Full (ML) / Total, filtros Todas / Solo Full / Activas / Sin ligar + buscador, tabla con publicación, tipo, precio, En Full (+ no disponibles con detalle), Publicado (para Colecta/Flex es solo lo que dice el anuncio, no inventario real), Producto del CRM, Estado. `obtenerResumenFull(resumenes)` alimenta también **`/stock`** (tarjeta "Valor de inventario" = bodega + Full con desglose, columna "En Full" en `TablaStock` vía `fullPorSku`) y el **dashboard `/`** (misma tarjeta). Todo solo lectura de ML; se publicó directo en `main`.
- **Sincronizaciones por tandas (para no pasarse del tiempo máximo de Vercel)**: Isaac reportó que "Actualizar" se tardaba mucho; las funciones de Vercel se cortan a los 10–60 s. Ahora ninguna llamada hace todo el trabajo: **Ventas** → `sincronizarVentasPagina({diasAtras, offset, desdeIso})` procesa 50 órdenes por llamada (8 en paralelo) y el botón (`boton-sincronizar.tsx`) va llamando página por página mostrando "N de M órdenes…"; `sincronizarOrdenes()` (auto-refresco de 2 días) usa el mismo mecanismo en bucle. **Stock** → `iniciarSyncStock()` (lista de IDs) → `sincronizarLoteStock(ids, primero)` en tandas de 40 (items en lotes de 20 en paralelo + inventarios Full en paralelo; `primero` borra la copia anterior) → `terminarSyncStock()`; el botón muestra "Publicaciones N de M…". Además `export const maxDuration = 60` en las páginas de Mercado Libre y en `research/nueva`. **La sincronización de ventas se puede reanudar**: Isaac preguntó qué pasa si cierra la laptop a medio "traer último año" (llevaba 8,000 de 20,000+). El botón guarda el avance en el navegador (`localStorage`, clave `daymart_ml_sync_ventas`: `{diasAtras, offset, desdeIso, guardadas, total}`) después de cada página; al volver a abrir la pantalla aparece un botón verde "Continuar donde se quedó (N de M)" que sigue desde ese punto, y se borra al terminar. Además `sincronizarPaginaOrdenes` se salta las órdenes que ya están guardadas sin cambios (mismo `last_updated` y con costo de envío ya traído), así una segunda pasada vuela. Sigue haciendo falta dejar la pestaña abierta mientras corre — el navegador es quien llama página por página. **Tope de 10,000 de Mercado Libre**: `/orders/search` rechaza `offset` ≥ 10,000 ("Limit must be a lower or equal than 10000") — a Isaac se le rompió "Traer último año" justo a la mitad (tiene 20,000+ ventas al año). Ahora `sincronizarPaginaOrdenes` trabaja por **ventanas de fecha**: cuando la siguiente página pasaría de 10,000, cierra la ventana por arriba con `order.date_created.to` = fecha de la orden más vieja de la página (+1 s) y reinicia `offset` en 0 (`hastaIso` viaja en el resultado y en el avance guardado); las órdenes de la frontera se repiten pero el upsert las absorbe. Si llega un avance viejo con offset 10,000 sin tope, se recupera el tope de la orden más vieja ya guardada en la base. El total general ("N de M") solo lo da la primera ventana y el botón lo conserva.
- **Pantalla de Stock ML ordenada por valor + actualización automática cada hora** (Isaac: "lo que vea arriba sea lo más importante", "que se actualice solo sin que yo haga nada"; migración 0033):
    - **Orden**: por `valorPublicacion` = precio de venta × piezas (`full_disponible` si está en Full, si no `cantidad_publicada`) — recomendación aceptada con "Va": así mil piezas de algo barato no le ganan a pocas de algo caro que en total vale más. Columna "Valor" visible. Las tarjetas de arriba siguen a COSTO (CRM); la lista va a PRECIO DE VENTA (existe para todas, ligadas o no).
    - **Un renglón por producto, no por publicación** (Isaac: "no quiero ver dos de cada uno"): la pantalla agrupa (`GrupoPublicacion`) las publicaciones que comparten stock — mismo `inventory_id` en Full, o ligadas por `relacion_item_id` fuera de Full (+ `variation_id`). La tradicional (`catalogo = false`) es la principal; las demás se anotan chiquito "+ catálogo MLM…". El producto cuenta como activo si cualquiera de sus publicaciones está activa; "Solo Full" si cualquiera es Full; el buscador busca en todas; "Ligar con producto" actúa sobre la publicación que ya trae liga (o la principal). Link discreto "Ver cada publicación por separado" (`?separadas=1`) para la vista una por una. El conteo "Sin ligar (N)" es por producto.
    - **Pausadas/finalizadas escondidas**: la tabla principal solo muestra `estado = active`; las demás van hasta abajo en un `<details>` colapsado "Pausadas y finalizadas (N)". Si hay texto en el buscador, se muestra TODO lo que coincida en una sola lista (sin esconder). Filtros: Todas / Solo Full / Sin ligar (se quitó "Activas", ya es el default).
    - **Sincronización por item, no borrón y cuenta nueva**: `sincronizarLotePublicaciones(ids)` ya no recibe `primero`: cada lote borra e inserta SOLO sus `item_id`, así la tabla nunca queda vacía a media pasada; `terminarSyncPublicaciones(inicioIso)` borra al final las filas con `actualizado_en < inicio` (publicaciones que ML ya no tiene). `iniciarSyncStock()` devuelve `inicioIso` del servidor para eso.
    - **Automático**: `avanzarSyncPublicaciones({presupuestoMs, cadaMinutos})` en `mercadolibre-stock.ts` guarda el avance en `mercadolibre_sync.stock_cursor` (`{ids, indice, inicio}`) y trabaja máx. ~40 s por llamada; solo arranca una pasada nueva si la última terminó hace > 55 min (cursor caduca a las 2 h por si se atora). La llama la ruta pública **`/api/mercadolibre/cron`** (GET, `maxDuration = 60`, en `RUTAS_PUBLICAS`), protegida por `CRON_SECRET` (variable de entorno en Vercel; se acepta `?clave=` o `Authorization: Bearer`, que es como la manda el cron de Vercel). Si le sobra tiempo, también atiende avisos del webhook y refresca ventas de 2 días (si la última sync tiene > 10 min). **Quién la llama**: el cron de Vercel en plan Hobby solo permite 1 vez al día, así que se usa **pg_cron + pg_net de Supabase** cada 10 minutos (SQL que corre Isaac, con su clave): `select cron.schedule('daymart-ml-cada-10-min', '*/10 * * * *', $$ select net.http_get(url := 'https://daymart-crm.vercel.app/api/mercadolibre/cron?clave=LA_CLAVE', timeout_milliseconds := 60000) $$);` — cada 10 min la ruta revisa; cada hora hace la pasada completa de stock. Para pausarlo: `select cron.unschedule('daymart-ml-cada-10-min');`.
- **Links de catálogo en Research** (`identificarLinkMercadoLibre` en `src/lib/mercadolibre.ts`): un link `mercadolibre.com.mx/<nombre>/p/MLM…` es una página de CATÁLOGO y ese ID es del producto genérico (`/products/{id}`), no de una publicación — pedir `/items/{id}` con él da 404 (le pasó a Isaac). Ahora: si el link trae `wid=MLM…` se usa esa publicación; si es `/p/MLM…` se consulta `/products/{id}` y se toma `buy_box_winner.item_id` (la publicación ganadora); si no hay ganadora, se avisa que pegue el link de una publicación concreta. `articulo.mercadolibre.com.mx/MLM-…` sigue siendo item directo.
- **Research: tres caminos para leer un anuncio** (`obtenerDatosMercadoLibre` en `src/lib/mercadolibre.ts`): aun con la cuenta conectada, ML respondió 403 al pedir `/items/{id}` de una publicación AJENA (desde 2024 la API restringe ver items de otros vendedores). Ahora se intenta en orden y se junta lo que cada uno dé: 1) `/products/{id}` (catálogo: nombre, foto, y de `buy_box_winner` precio/categoría/tipo de publicación); 2) `/items/{id}` con el token (ventas, categoría, precio); 3) **la página pública del anuncio** leída como navegador (`leerPaginaPublica`: JSON-LD `@type: Product` → nombre/imagen/precio, `og:*` como respaldo, y `"category_id":"MLM…"`/`"listing_type_id"` buscados en el HTML) — probado con un HTML simulado. Solo si nada da el nombre se muestra error, con el detalle de qué respondió cada camino (ej. "publicación MLM…: 403") para poder diagnosticar. Como desde este entorno NO se puede llamar a Mercado Libre (egress bloqueado), el mensaje de error incluye el `message` que devuelve la API (`pedirJson` → `detalle`) y la página pública se intenta con varias URLs (el link pegado, `articulo.mercadolibre.com.mx/MLM-{id}-_JM`, `www.mercadolibre.com.mx/p/{productId}`) con encabezados de navegador; Resultado real (17 sep): los anuncios PROPIOS y los de CATÁLOGO ya jalan; los de OTROS vendedores dan `403 Access to the requested resource is forbidden` en `/items/{id}` y la página pública regresa ~26 KB sin datos (muro anti-robots de ML, no se intenta evadir). Por eso: (a) `identificarLinkMercadoLibre` ahora devuelve también `productoId` cuando el link trae `/p/MLM…` Y `wid=` — se lee el catálogo como respaldo aunque la publicación se niegue; (b) si `/items/{id}` falla se intenta el lote `/items?ids=` y, si el item trae `catalog_product_id`, se completa con `/products/{id}`; (c) **"Detectar categoría por el nombre"** (botón bajo "Nombre del producto", `categoriaPorNombreMl` → `predecirCategoriaMercadoLibre` → `/sites/MLM/domain_discovery/search?q=`): con el nombre copiado a mano se adivina la categoría y se consulta la comisión real — el mensaje de error del 403 ya le dice a Isaac que use ese camino.
- **Stock al 100% con Mercado Libre — `/stock/full` "Full y ML"** (migración 0035, `src/lib/salidas-ml.ts`; plan aprobado con "va" el 18 sep; Isaac: "que nunca me falte una pieza"). Regla de oro: nada sale de bodega sin motivo registrado; cada salida automática queda ligada a su origen (`movimientos_stock.orden_ml_id` / `envio_full_id` / `recepcion_full_id`) y por eso NO se edita en `/stock/movimientos` (`movimientoSuelto` la bloquea, el listado la liga a `/stock/full`).
    - **Envíos a Full** (`envios_full` + `envios_full_lineas`): Isaac arma el envío (productos, piezas, color de etiqueta, fecha, bodega) → estado PREPARADO = "en camino a Full", la bodega NO se descuenta todavía (así lo pidió). Se cierra solo (RECIBIDO) cuando todas sus líneas quedan resueltas.
    - **Detección de recepciones** (`registrarHistorialFull`, se dispara al final de CADA sincronización de publicaciones, manual o del reloj): guarda en `mercadolibre_full_historial` el total (disponible + no disponible) por `inventory_id` cuando cambia; si el total SUBE respecto a la fila anterior, crea una `mercadolibre_full_recepciones` (la primera vez que se ve un inventario solo es base). La docs de ML confirman que el stock de Full y sus operaciones (inbound_reception) se pueden consultar por API, pero desde este entorno no se pudo leer la documentación (egress bloqueado) — por eso la detección se hace por diferencia de totales, que solo usa datos que ya sincronizamos. Mejora futura: `/stock/fulfillment/operations/search` (tipo `inbound_reception`) y el tópico de notificaciones `stock_locations` / `fbm_stock_operations`.
    - **Recibir el envío COMPLETO de un jalón** (23 sep; Isaac: "es imposible estarle dando que sí producto por producto; lo manejo por envío completo"): en cada envío PREPARADO hay un botón "Dar por recibido el envío #N" (`confirmar-envio.tsx` → `recibirEnvioCompleto` → `recibirEnvioFullCompleto` en `salidas-ml.ts`): lista sus líneas con "enviadas" y la pista "ML detectó N" (suma de recepciones pendientes por SKU), campo "llegaron" (por defecto todo) y, si llegó menos, "se quedaron en bodega" (ajusta lo enviado) o "merma" (SALIDA destino Merma). Un solo clic genera las SALIDAS destino "Full" de todas las líneas (`envio_full_id`, referencia "Envío a Full #N recibido"), marca las líneas resueltas, cierra el envío (RECIBIDO) y marca atendidas (decision SALIDA, `envio_id`) las recepciones detectadas de esos SKUs para que no se vuelvan a proponer. Los envíos en camino van primero. Las detecciones por producto ("¿Se recibió este Full?") ya NO van arriba: quedan en un `<details>` colapsado al final de la sección de envíos, solo para lo que no cuadre con ningún envío (ej. devolución a Full).
    - **"¿Se recibió este Full?"** (`TarjetaRecepcion` → `atenderRecepcionFull`, ahora secundario): "Sí, salió de mi bodega" crea la SALIDA (destino "Full", cantidad editable, SKU editable si no estaba ligado) ligada a la recepción y al envío PREPARADO más viejo que traiga ese SKU (o al que Isaac elija), suma `cantidad_recibida` a la línea; "No fue de mi bodega" la marca IGNORADA (ej. devolución de un comprador a Full). Diferencias (ML recibió menos): `resolverDiferenciaLinea` → "se quedaron en bodega" (ajusta lo enviado) o "merma" (SALIDA destino "Merma").
    - **Ventas de ML que salen de la bodega** (`procesarVentasMl`, corre en el reloj `/api/mercadolibre/cron` y con "Revisar ahora"): órdenes `paid`, logística ≠ Full (`LOGISTICAS_BODEGA`), envío en `shipped/delivered/not_delivered` (ya salió físicamente), `fecha_creacion ≥ configuracion_stock.salidas_ml_desde` (interruptor con fecha de arranque = día del conteo físico; null = apagado), sin `salida_generada_en` → una SALIDA por renglón (destino "Mercado Libre", `creado_en` = fecha de la venta, `orden_ml_id`). Se marca `salida_generada_en` ANTES de insertar (y se desmarca si falla) para que dos relojes no dupliquen. Si algún renglón no resuelve a SKU del CRM (liga manual `mercadolibre_vinculos`, automática por `seller_sku`, o publicación), la orden queda en "esperando liga" (no se pierde; al ligar se genera sola).
    - **Devoluciones**: orden `cancelled` que ya tenía salida → `devolucion_estado = POR_CONFIRMAR`; Isaac confirma "Regresó a bodega" (AJUSTE + con costo promedio, `orden_ml_id`) o "No regresó / dañada" (MERMA, queda fuera). Las ventas de Full se devuelven a Full, no tocan bodega.
    - Pantalla: tarjetas de recepciones, envíos (con avance confirmadas/enviadas, diferencias, cancelar si nada se ha confirmado), interruptor de salidas automáticas, ventas esperando liga (link a `/mercadolibre/stock?filtro=sinligar`), devoluciones por confirmar, últimas salidas por ventas. Pestaña "Full y ML" en `NavStock`.
    - Decisiones de Isaac (18 sep): ventas no Full automáticas sin preguntar; sueldo SÍ es gasto del negocio (para Maaser automático, pendiente); ganancia de ML se reconoce cuando la orden está pagada. Pendiente: Maaser automático (ventas directas + ML − gastos marcados), columna "en camino a Full" en `/stock`, mejora con la API de operaciones de Full.
- **Montos del cobro real (migración 0037, `src/lib/mercadolibre-montos.ts`)** — caso real del 20 sep: la app de ML mostraba "pagó el comprador $184.37, comisión 15% $27.66 a cargo de ML, envío $40 a cargo de ML, total a recibir $116.71", y el CRM decía "Total vendido $117, comisión $0, te queda $77". Con el esquema nuevo de facturación de México (ML le factura al comprador, Isaac le factura a ML el neto), la orden llega con `total_amount` = lo que le llega a Isaac y `sale_fee` = 0: la comisión y el envío van "por dentro". Ahora `guardarOrden` lee también `payments[]` de la orden (`montosDePago`: `total_paid_amount`/`transaction_amount` → `pagado_comprador`, `shipping_cost` → `envio_comprador`, `marketplace_fee` → `comision_mp`; solo para órdenes sin `pack_id`, porque en un carrito cada orden trae el mismo cobro del carrito completo) y guarda `comision` = la de Mercado Pago si vino, si no la de `sale_fee`. En pantalla (`totalVendidoOrden`/`comisionOrden`/`netoOrden`): vendido = pagado − envío del comprador (nunca menor al total de la orden, un cupón lo paga ML); si la comisión guardada es 0 pero el comprador pagó más que el valor del producto, esa diferencia menos el envío a cargo del vendedor es la comisión (mientras ML no publique el envío, la diferencia completa se muestra como comisión y el "te queda" sale bien igual). La tabla "Por producto" reparte el vendido/comisión de la orden entre sus renglones en proporción a su valor (`parteDelRenglon`). Las órdenes ya guardadas se completan desde su `payload` sin llamar a ML (`completarMontosGuardados`: 200 al abrir Ventas, tandas de 300 en el reloj; `montos_revisado_en` marca las ya revisadas). **Detalle por orden `/mercadolibre/ordenes/[id]`** (link en el número de la orden): desglose "Pagó el comprador / Valor de la venta / Comisión (y de dónde salió) / Envío a tu cargo / Te queda / Valor del producto según ML" y un `<details>` con los datos crudos de la orden (total_amount, paid_amount, unit_price/full_unit_price/sale_fee por renglón, y por cobro transaction_amount/total_paid_amount/shipping_cost/marketplace_fee) — si algo no cuadra, Isaac manda captura de eso. No se pudo verificar contra la API desde este entorno (egress bloqueado): la regla se armó con los números de la captura de Isaac y está probada con esos casos (`scratchpad`), pendiente confirmar con su orden real.
- **Actuar dentro de Mercado Libre — Fase 1: Promociones con margen real + Resumen del día** (25 sep; Isaac: "también haz lo que me preguntaste de ML"; migraciones 0040 y 0041). Primera ESCRITURA en ML: reglas fijas — nada automático (Isaac confirma cada cambio con un botón viendo antes el resultado), todo queda en bitácora con "Quitar promoción", y **la aplicación necesita permiso de escritura ("write") en developers.mercadolibre.com.mx/devcenter** (si ML contesta 403, `mercadolibrePost`/`mercadolibrePut`/`mercadolibreDelete` lo dicen). Desde este entorno no se puede llamar a ML ni leer su documentación (egress bloqueado): los campos de `/seller-promotions` se armaron de memoria y se leen a la defensiva, cada promoción muestra "datos crudos" para diagnosticar con Isaac en la primera prueba.
    - **REGLA DE ISAAC (25 sep): "el cambio de precios solo en la parte de promociones — que se aplique una promoción, no que me cambie el precio de la publicación"**. Por eso el CRM NUNCA hace `PUT /items/{id} {price}`: un precio más bajo se aplica siempre como promoción (`POST /seller-promotions/items/{item_id}?app_version=v2`), el precio base queda intacto y la promoción se puede quitar (`DELETE …?promotion_type=&promotion_id=`). Tipos (`TIPOS_PROMOCION` en `src/lib/mercadolibre-promociones.ts`): `PRICE_DISCOUNT` = "Descuento del vendedor" (lo crea Isaac: `{promotion_type, deal_price, start_date?, finish_date?}` con fechas ISO en horario CDMX vía `isoFechaMx`; sin fin = hasta que la quite), y las campañas que ML ofrece por publicación (DEAL, LIGHTNING, DOD, MARKETPLACE_CAMPAIGN, SELLER_CAMPAIGN, SMART, PRE_NEGOTIATED…: `{promotion_id, promotion_type, deal_price?}`). `promocionesDeItem(itemId)` = `GET /seller-promotions/items/{id}?app_version=v2` normalizado (id, tipo, estado candidate/started/pending…, fechas, precio sugerido/mín/máx, % que pone ML en `benefits.meli_percent`, crudo).
    - **Datos nuevos en `mercadolibre_publicaciones`** (0040): `categoria_id`, `tipo_publicacion` (gold_special = Clásica / gold_pro = Premium), `precio_original` (ML manda `price` = precio con promo y `original_price` = base cuando hay promoción activa), `comision_pct` y `comision_fija` — comisión real de ML al precio actual (`/sites/MLM/listing_prices?price&category_id&listing_type_id`, caché por corrida, solo activas) guardada al sincronizar (`comisionDe` en `mercadolibre-stock.ts`). Sin el SQL 0040 el insert reintenta sin esas columnas (`COLUMNAS_0040`). `mercadolibre_sync.margen_minimo_pct` (20 por defecto). Tabla `mercadolibre_cambios_precio` = bitácora; 0041 le agrega `accion` (APLICAR|QUITAR), `promocion_tipo`, `promocion_id`, `fin_promocion` (sin 0041 se inserta sin esas columnas, `COLUMNAS_0041`).
    - **Cálculo** (`src/lib/mercadolibre-margen.ts`, puro): comisión = precio × pct + fija; **te queda = precio − comisión − envío − costo**; margen % = te queda ÷ precio; `precioParaMargen` = (fija + envío + costo) ÷ (1 − pct − margen). **Envío** = promedio real por pieza que pagó Isaac en las ventas pagadas de ESA publicación en 90 días (`envioPromedioPorItem`, reparte `costo_envio_vendedor` entre las piezas de la orden; sin dato = "—" y se toma 0). **Costo** = costo promedio del SKU del CRM ligado (sin liga = sin margen).
    - **Pantalla `/mercadolibre/promociones`** (pestaña "Promociones"; `tabla-promociones.tsx`): margen mínimo editable (`margen-minimo.tsx`); modo "Bajar %", "Precio fijo" o "Que me deje X% de margen" + valor + "Hasta (opcional)"; tabla de publicaciones ACTIVAS con precio BASE (y "en promo: $X" si ya tiene descuento), comisión, envío, costo, te queda (rojo si < mínimo) y, al seleccionar, "Con descuento" y "te quedaría". El descuento se calcula sobre el precio base y **solo puede bajarlo** (los que no bajan se ignoran y se avisa). "Aplicar N promoción(es)" confirma con la lista base → descuento (% off) y vigencia, y manda tandas de 10 a `aplicarPromocionesMl` (máx. 15 por llamada) → `aplicarPromocion` tipo PRICE_DISCOUNT; lo que quede abajo del mínimo no se aplica salvo "aplicar de todos modos". Bajo cada renglón, **"Promociones de ML ▸"** (`promociones-item.tsx`, una llamada al picarle): campañas candidatas con precio sugerido editable (rango mín–máx), % que pone ML, margen a ese precio → "Participar"; activas → "Quitar". `bitacora.tsx` → "Quitar promoción" (`deshacerPromocionMl` = DELETE y `deshecho_en`).
    - **Resumen "Para hoy"** (`resumen-dia.tsx`, arriba de Ventas en `/mercadolibre`, solo si hay algo): (1) **Full se va a quedar sin stock**: piezas en Full ÷ (vendidas en 30 días ÷ 30) ≤ 14 días, un renglón por inventario, con lo que hay en bodega y link a armar envío; (2) **Abajo del margen mínimo** → link a Promociones; (3) **Sin ventas en 14 días con stock**, por valor parado; (4) **Preguntas sin responder** (`/my/received_questions/search?status=UNANSWERED`). Máx. 6 por tarjeta.
    - Pendiente de la misma propuesta: pausar/activar y cantidad publicada, reabastecer Full con un clic, responder preguntas con IA. Confirmar en la primera prueba real los campos de `/seller-promotions` (Isaac manda captura de "datos crudos" si algo no cuadra).
- **Fase C — "Nuevo producto con estos datos" desde Publicaciones** (25 sep, Isaac: "si el producto no lo tengo registrado, que me jale la foto y la información de ML, yo solo pongo el precio y el histórico"). En `LigaProducto` (`liga-producto.tsx`), junto a "Ligar con producto" y dentro del selector, el link **"+ Nuevo producto con estos datos"** abre `nuevo-producto-ml.tsx` (`NuevoProductoMl`): foto y nombre precargados de la publicación (variante precargada del texto de la variación), marca (Selector, Daymart por defecto), categoría (`CampoSugerencias`), SKU armado en vivo con `skuNuevo` + `skuLibre` (editable; muestra el SKU que tiene en ML y recomienda ponerlo igual), piezas por caja, costo por pieza, y casilla "Ya tengo stock (cargar su histórico)" con entradas/salidas totales, fecha de corte y bodega. `crearProductoDesdeMl` (mercadolibre/actions.ts): rechaza SKU existente, **copia la foto de ML al bucket `productos`** (`copiarImagenMl`, carpeta `mercadolibre/`; si falla deja el link de ML), con histórico llama a `registrarProductoStock` (mismo camino que "+ Agregar producto") y sin histórico solo `guardarEnCatalogo`, y liga la publicación elegida **y las que comparten su stock** (`otras` del grupo) en `mercadolibre_vinculos`. La página de Publicaciones carga marcas, categorías, bodegas y SKUs existentes (`catalogoParaNuevo`) para ese formulario.
- **La pestaña `/mercadolibre/stock` se llama "Publicaciones"** (23 sep, Isaac: "se ve mejor"); el título de la pantalla es "Publicaciones en Mercado Libre". La ruta no cambió.
- **Pendiente**: unificación de SKUs (Isaac); confirmar la regla de `sale_fee` con una venta real.

## Respaldo en Google Sheets (Finanzas + Stock + Contenedores)

Isaac quería un respaldo fuera del sistema por si algún día falla. Mecanismo: `/api/respaldo` (`src/app/api/respaldo/route.ts`) es una ruta de solo lectura protegida por una clave secreta (`RESPALDO_SECRET`, variable de entorno en Vercel) — **no usa login normal** porque quien la llama es un script de Google, no un navegador con sesión. Por eso mismo usa la **service role key** de Supabase (`SUPABASE_SERVICE_ROLE_KEY`, nunca la anon key) del lado del servidor — la anon key es pública (viaja en el navegador) así que jamás se le puede dar permiso directo a estas tablas sensibles; la única puerta de entrada es la clave secreta de esta ruta. `src/proxy.ts` excluye `/api/respaldo` del login normal a propósito (`RUTAS_PUBLICAS`).

Devuelve tres bloques en JSON: `finanzas` (libro completo de movimientos_financieros con nombres de cuenta/categoría ya resueltos), `stock` (resumen actual por SKU — cantidad, cajas, costo y valor, no el historial completo), `contenedores` (lista con estado, booking, fábrica/proveedor y costo). Un script de Google Apps Script (que vive en la hoja de Isaac, fuera de este repo) llama esta ruta cada cierto tiempo (Isaac eligió que sea todo: Finanzas + Stock + Contenedores) y escribe cada bloque en su propia pestaña de la hoja.

## Módulo Research (en pruebas — NO publicado todavía en la página real)

Solo dueño (trae números de costo/margen). Isaac lo diseñó platicándolo primero conmigo, con su técnica real de buscar productos en Mercado Libre antes de decidir si vale la pena importarlos. Por su propia petición, este módulo se construyó primero en una rama de trabajo aparte y una vista previa de Vercel, sin tocar `main`/su página real, hasta que él lo pruebe con un producto de verdad y confirme que los números cuadran.

**Flujo:**
1. Isaac pega el link de un producto de Mercado Libre (`/research/nueva`) y le da "Traer datos" → el sistema jala foto, nombre, precio de referencia, categoría y ventas del anuncio, usando la API pública de Mercado Libre (`api.mercadolibre.com/items/{id}`, sin necesidad de sesión) — el ID se saca del link con una expresión regular (`src/lib/mercadolibre.ts`). Si falla (producto no encontrado, sin conexión), no truena la pantalla — se queda todo editable a mano.
2. Isaac llena lo que solo él sabe: precio de compra en dólares, tipo de cambio estimado, piezas por caja, medidas de la caja de importación, y su $/CBM estimado (de su experiencia, no exacto) → de ahí sale el costo estimado por pieza, con la MISMA fórmula que ya usa Contenedores (`src/lib/calculos-research.ts`, reutiliza `cbmProducto` de `src/lib/calculos.ts`).
3. **Ojo con esta distinción**: las medidas de "la caja" para el costo de importación (arriba) NO son las mismas que las del "paquete individual" (como le llega a UN cliente de Mercado Libre) que se usan para calcular el costo de envío de Mercado Libre — son dos cajas distintas (una trae varias piezas para importar, la otra es una sola pieza para el cliente final). El formulario pide las dos por separado.
4. Con las medidas del paquete individual, se calcula el peso volumétrico (`(largo × ancho × alto cm) ÷ 5000`, fórmula oficial de Mercado Libre) y se compara contra el peso físico real si Isaac lo puso (siempre se usa el mayor de los dos) → se busca en la tabla oficial de costos de envío de Mercado Libre México (`src/lib/costos-envio-ml.ts`, la tabla completa con reputación verde que mandó Isaac) → se propone el costo de envío, **editable a mano**.
5. La comisión de Mercado Libre por ahora se captura A MANO (Isaac la saca del simulador real: https://vendedores.mercadolibre.com.mx/simulador-de-costos) — no se automatizó porque no se pudo probar en vivo la API de comisiones de Mercado Libre antes de construir esto (limitación del entorno de trabajo, no de Mercado Libre) — queda pendiente para automatizar después, una vez confirmado que los números del resto ya cuadran.
6. Con todo eso: margen estimado en vivo (pesos y %), antes de guardar.
7. Botón "Guardar como borrador" (`research_productos`, migración 0020) — guarda todo como foto del momento, no se recalcula solo después.
8. Desde un borrador, "Convertir en producto →" — mismo patrón que "agregarProducto" de Contenedores, crea el producto real en el contenedor que Isaac elija (con categoría/fábrica/proveedor/cantidad que confirme ahí) y marca el borrador como `CONVERTIDO`, ligado a ese contenedor.

**Pendiente después de que Isaac lo pruebe**: automatizar la comisión de Mercado Libre (hay una API pública `sites/MLM/listing_prices` que probablemente la calcula, sin confirmar en vivo todavía); permitir editar un borrador ya guardado (hoy solo se puede guardar, descartar o quitar).

## Lo que se deja para después (NO hacer todavía)

- Login / usuarios / permisos por colaborador. Isaac ya confirmó que por ahora solo él usa el sistema, así que no hace falta todavía — pero es un REQUISITO antes de construir la "firma" de salidas con encargado: sin cuentas de usuario no hay forma de saber quién hizo qué, y eso es justo lo que Isaac pidió para prevenir robo hormiga. Cuando llegue el momento: dos roles (Administrador / Encargado de bodega), cada salida queda con usuario+fecha, no se puede editar/borrar después (solo cancelar con motivo), y una pantalla de auditoría para que Isaac revise al encargado.
- Estudio de mercado, predicción de ventas, comparación "cómo me fue vs. lo esperado", temporadas por categoría.
- Ventas, conexión con Mercado Libre / Amazon MX (Módulo 3) — incluye sincronizar "Stock en Full" y reconciliar salidas contra lo que ML confirmó recibido.
- **Importar el historial de los Excels de Isaac sin tocar los datos reales** (20 sep, Isaac: "recuérdame más adelante y lo hacemos"). Propuesta ya explicada y pendiente de su "va": fecha de arranque del sistema; cada dato histórico entra marcado con un lote de importación (no toca saldos de cuentas ni stock, solo sirve para métricas por mes/categoría/proveedor) y se puede deshacer por lote; primero la hoja de movimientos de "Finanzas_Daymart" como CSV adjunto (no pegado); ventas de ML no hacen falta (ya están sincronizadas); contenedores < 10 en segunda vuelta; stock histórico no.
- Migrar el histórico de contenedores anteriores al 10 (Isaac los borró del Excel por pesado; se agregarán después cuando el sistema esté listo).
